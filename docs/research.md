# Research and optimization decisions

Sources inspected on 2026-09-20:

- [Original MOSS model card](https://huggingface.co/OpenMOSS-Team/MOSS-Audio-Tokenizer): identifies the requested 24 kHz checkpoint and streaming API constraints. Do not substitute the newer v2 model. Local pinned source is authoritative for exact arithmetic and cache behavior.
- [MOSS paper](https://arxiv.org/abs/2602.10934): architecture/training background; its claims do not establish speedups for this implementation.
- [PyTorch CUDA graphs article](https://pytorch.org/blog/accelerating-pytorch-with-cuda-graphs/) and [CUDA semantics documentation](https://docs.pytorch.org/docs/main/notes/cuda.html): motivate eliminating repeated host launch overhead while retaining stable graph tensor addresses. Capture setup is measured separately; output clones prevent replay aliasing.
- [Triton layer normalization tutorial](https://triton-lang.org/main/getting-started/tutorials/05-layer-norm.html): reduction and fusion reference. A new reduction order can change codec decisions; normalization fusion is not yet promoted.
- [CuTe DSL documentation](https://docs.nvidia.com/cutlass/4.3.2/media/docs/pythonDSL/cute_dsl.html) and [NVIDIA elementwise tutorial](https://github.com/NVIDIA/cutlass/blob/main/examples/python/CuTeDSL/cute/ampere/tutorial/elementwise_add_autotune.py): launch/framework integration reference. Implementation uses installed CuTe 4.7.1 API. SM100-only tensor instructions are inappropriate for this GeForce SM120 GPU.

The residual kernel uses two rounded FP32 operations, rather than FMA, to match upstream multiply then add. Triton sets `enable_fp_fusion=False`; CuTe emits `mul.rn.f32` followed by `add.rn.f32`. Cancellation tests exercise this difference.

Initial reduced-model profiling identifies 66 weight-normalization calls per encode, accounting for 18.3% of recorded device operator time in that fixture. Its quantizer dimensions/depth are retained, but transformer depth/width are reduced: percentages must not be extrapolated to the full checkpoint. Frozen weight materialization and codebook normalization are now cached without changing operations or parameter tensors.

Future investigations: repeated RoPE tables/masks across transformer layers; fused ring-cache updates; strict-FP32 quantizer nearest-neighbor kernels preserving near-tie decisions; the many small 1x1 quantizer convolutions; GEMM shapes and bandwidth limits of 6+ GB FP32 parameters. Measure each change in isolation, reject fidelity failures, and retain regressions in the evidence log.

Confirmed streaming defect: upstream `RingKVCache.complete` writes a whole chunk before attention into a ring of `context` entries. With multiple local tokens per chunk, this overwrites history needed by early queries once the ring fills. A uniform-attention regression demonstrates an incorrect average of 2.5 instead of 2.0. Sessions now allocate `context + chunk_tokens - 1` entries at each transformer resolution, retaining the unchanged context mask. Tests verify both the analytic counterexample and capacity throughout encoder/decoder stages. Full-checkpoint long-duration comparisons against offline causal attention remain required; matching corrected eager streaming alone is insufficient.

## Full-checkpoint findings and new kernels

The completed 80 ms full-checkpoint profile records 306 encode matrix multiplications (5.30 ms, 49.1% of device operator time) and 273 decode matrix multiplications (5.26 ms, 55.8%). Unprofiled wall times are 46.15/45.17 ms, showing substantial host-launch gaps. Quantizer weight normalization is only 1.4% of full encode device time; the earlier 18.3% fixture percentage was not representative of the whole model.

The RoPE implementation emitted roughly a thousand elementwise multiplications per encode. A new Triton kernel rotates Q and K together, using PyTorch's original sin/cos results and separate FP32 arithmetic to retain bitwise equality. Static frequencies are cached. Full-model ablation on 240 ms inputs reduces graph encode from approximately 11.8 to 10.5 ms and decode from 10.0 to 8.7 ms. Exact corpus checks pass with both Triton and CuTe residual kernels.

The new two-launch Triton KV path preserves active-lane values/positions, skips writes for paused lanes, and advances offsets on device. Amortized cache-update measurements improve approximately 2.7–14.7× depending on batch/cache shape; these are component timings. Full two-lane pause/resume/reset tests match independent timelines exactly. A 12.8-second full-model music test has exact streaming/offline token equality. Decoder streaming/offline differences (maximum 1.70e-6, RMSE 4.99e-8) already exist in corrected eager execution; the optimized stream is bitwise identical to that eager reference. Broader contexts/content still need validation.

Additional primary sources: [Triton fused attention tutorial](https://triton-lang.org/main/getting-started/tutorials/06-fused-attention.html), [NVIDIA PTX floating-point semantics](https://docs.nvidia.com/cuda/parallel-thread-execution/), and [NVIDIA RTX 5070 Ti bandwidth specification](https://blogs.nvidia.com/blog/studio-ai-geforce-rtx-5070-ti-gpu-dlss/). A new attention reduction would need new numerical evidence; the fused-attention example's lower-precision performance is not an FP32 result.

`results/weight_storage.json` records a checkpoint storage inspection. The sampled matrices are not exactly representable in FP16/BF16, so simply casting weights would violate bitwise preservation. At the published 896 GB/s peak bandwidth, reading all dense FP32 transformer matrices once per direction alone takes about four milliseconds. This is an inference under the dense, per-call weight-read assumption, not a universal bound on every future algorithm or hardware choice. It explains why a 100× claim against a roughly 40–50 ms GPU baseline is unsupported by current evidence. Continue with matrix-kernel profiling, reduction-preserving fusion, table/mask sharing, and measured batching rather than changing the baseline to manufacture a speedup.


## Shared tables, matrix experiments, and batching

Stage-shared RoPE tables now retain exact outputs in full-model offline, long-stream, and independently paused/reset lane checks. Sharing requires known synchronized offsets; external streaming with divergent offsets falls back. The 240 ms batch-one graph improves to 10.158 ms encode / 8.391 ms decode. Causal-mask sharing remains a separate candidate.

Actual checkpoint matrix experiments use the [Triton matrix multiplication tutorial](https://triton-lang.org/main/getting-started/tutorials/03-matrix-multiplication.html), [tl.dot precision documentation](https://triton-lang.org/main/python-api/generated/triton.language.dot.html), and [CUTLASS functionality table](https://docs.nvidia.com/cutlass/latest/media/docs/cpp/functionality.html) as implementation references. `input_precision="ieee"` avoids TF32 input truncation but does not force the vendor reduction order. Naive tiled variants generally regress. Specialized row reductions sometimes improve warm-cache GEMV substantially; gains fall to roughly 1.1–1.3× with cache eviction. Their full-model experiment offers only about 1.7% combined latency improvement and changes waveform rounding despite exact tokens on 11 short inputs. These paths remain experimental, with numerical differences recorded rather than inferred to be perceptual quality loss.

NVIDIA's [floating-point emulation article](https://developer.nvidia.com/blog/unlocking-tensor-core-performance-with-floating-point-emulation-in-cublas/) describes BF16x9 FP32 emulation using multiple components/cross-products. The [cuBLAS documentation](https://docs.nvidia.com/cuda/cublas/) support table inspected on 2026-09-20 restricts this floating-point FP32 route to compute capabilities 10.0/10.3 with CUDA 12.9 or later. This RTX 5070 Ti is SM12.0 and is not listed. Fixed-point FP64 emulation support on 12.x is a different feature. Do not assume upgrading CUDA provides BF16x9 here; no such path or reduced-precision weights have been enabled.

Matched batching demonstrates a distinct throughput/latency tradeoff: batch 128 reaches 353.5/364.1 audio seconds per second encode/decode while only improving 1.06×/1.08× over equal-batch upstream eager. Its profile is roughly 80% SIMT SGEMM and 10% efficient attention. Thus matrix arithmetic dominates at larger batches, while graph launch elimination explains much of the small-batch gain. Both measurements guide further work; neither supports a 100× matched-workload claim.


## Fused and shared attention masks

The pinned upstream attention builds per-layer boolean masks from 64-bit query/key positions. A Triton kernel now computes the same causal/context tests and can write the FP32 additive bias directly, with row strides aligned to eight elements. `attention_mask_backend="triton"` enables it; synchronized stage calls reuse the bias while retaining separate KV tensors. External upstream streaming uses per-layer masks. Query/key/value projection, rotary arithmetic, SDPA, and output projection remain unchanged.

The [PyTorch 2.8.0 attention implementation](https://github.com/pytorch/pytorch/blob/v2.8.0/aten/src/ATen/native/transformers/attention.cpp) confirms that its ordinary boolean-mask conversion uses zero and negative infinity, followed by eight-element alignment for memory-efficient attention. Directly constructing these values avoids conversion and padding on every layer. This applies to the tested CUDA FP32 route; cuDNN's separate boolean conversion uses a finite sentinel, so do not generalize this implementation to reduced-precision/cuDNN paths. [SDPA documentation](https://docs.pytorch.org/docs/main/generated/torch.nn.functional.scaled_dot_product_attention.html) describes the boolean/additive semantics.

A shared-boolean intermediate reached 9.719/7.945 ms graph encode/decode; direct aligned additive construction reached 9.491/7.821 ms. The matched full upstream eager baseline for the final run was 48.369/38.571 ms, giving 5.10×/4.93×. Both residual backends retain exact outputs in the 12-case corpus, and corrected long streaming remains exact. Changes to attention reductions have not been introduced.

The new filled-ring profile (batch two, 80 ms chunks, full ten-second history) measures 11.665 ms encode and 10.208 ms decode per push. Dense GEMM kernel groups take roughly half the device time; memory-efficient attention takes 26.6% encode / 29.7% decode. Therefore streaming attention is now a substantial optimization target alongside weight-bandwidth-limited small-M matrices. Repeated cache offset operations remain candidates, but their individual kernels are below 1% each in this profile.


## FP32 split-cache attention research

Actual filled-ring activations confirm four attention geometries per direction at batch two / 80 ms chunks: (heads, queries, keys, width) = (20,1,125,64), (12,2,251,64), (12,4,503,64), (12,8,1007,64). The first geometry occurs in 32 layers; each other geometry occurs in 12. `benchmarks.attention_shapes` captures representative inputs after the ring fills, preserving their strides, and compares vendor SDPA, its math backend, an explicit FP32 Triton split-cache reduction, and optional compiled FlexAttention. Warm-cache and cache-evicted component times are separated from model measurements.

The [Flash-Decoding article](https://pytorch.org/blog/flash-decoding/) motivates splitting the key/value sequence to increase parallel work when query length is short. Its published lower-precision long-context speedups are not this codec's FP32 performance. Our prototype computes per-split maxima, denominators, and weighted-value sums, followed by a stable merge. Fully masked rows return zero. No TF32, FP16/BF16 operands, FP8/FP4, or weight conversion is used. Reduction order and exponential rounding differ from vendor SDPA, so this is an experimental numerical alternative, not an exact replacement.

`results/attention_shapes.json` records roughly 3–7× warm component improvements for the 64-key split, with smaller gains after cache eviction. Against FP64 diagnostics, error is often smaller than vendor FP32 but not uniformly so; closer-to-FP64 component results do not prove codec quality. The [FlexAttention inference article](https://pytorch.org/blog/flexattention-for-inference/) supplied a second comparison. Installed PyTorch 2.8 uses a quoted source literal for its precision option (`"'ieee'"`); the experiment explicitly requests it and disables TF32. FlexAttention is faster than SDPA here but slower than the custom split prototype, and is also nonbitwise (`results/attention_flex.json`).

Full streaming measurements use music, speech, and environmental audio at 12.8 seconds per lane; recordings shorter than that are explicitly repeated, with repetition counts in the report. Across two lanes and all 32 quantizers, all 30,720 tokens match the existing optimized stream. With fixed reference codes, maximum waveform differences are 1.69e-6 music, 2.10e-5 speech, and 8.51e-5 environmental audio, with respective signal-to-error ratios of 126.4, 118.2, and 102.0 dB. This is numerical evidence, not a listening study or universal no-quality-loss result. The prototype stays outside the supported runtime.

The prototype improves encoder latency from about 11.6–11.8 to 9.35–9.37 ms and decoder from about 10.13 to 7.84–7.85 ms per filled-ring push (1.24–1.26× / about 1.29× against the already optimized graph). These are approximately 20–21% / 22–23% latency reductions, not another 5× whole-model result. Further work should tune query reuse, examine near-tie quantizer decisions on more diverse recordings, and investigate attention reduction order if bitwise replacement is required.

A targeted test exposed stride-dependent Triton reduction trees when graph cloning densifies gapped Q/K/V tensors. The experimental wrapper now canonicalizes Q/K/V layout before launch and prevents specialization on bias row strides, which also affected the 256-key variant. Captured model buffers are already contiguous and require no copies; gapped caller inputs pay explicitly for normalization. Eager/graph equality is verified separately from tolerance-based comparison with vendor SDPA.


## Independent final lengths and fused lane reset

`StreamingSession.push` now accepts host `valid_lengths` and `final_lanes` sequences. A Triton preparation kernel masks invalid source positions before loading them, writes zero padding, computes effective lengths, and combines requested activity with persistent completion state. This avoids device-to-host reads for scheduling and safely ignores NaN audio or invalid code IDs in padding. A final partial encoder frame is retained; callers still track original sample counts for waveform trimming. Continuing lanes advance full chunks; partial positive tails must finish their lane. Empty completion skips model execution. Completed lanes remain inactive until reset, and the captured graph is reused.

Reset previously traversed every state and launched many small zero/where/copy operations. The new reset plan validates known state types, contiguous per-lane int64 offsets, and shared execution masks, then stores their addresses in a GPU table. One masked Triton kernel clears selected offsets and reopens selected lanes; unselected state remains unchanged. CPU offset bookkeeping retains upstream semantics. Unknown layouts/states fall back to original reset methods, and `fast_reset=False` is available for diagnostics. The plan releases state references on exit. [Triton cast documentation](https://triton-lang.org/main/python-api/generated/triton.language.cast.html) and [masked store semantics](https://triton-lang.org/main/python-api/generated/triton.language.store.html) are the relevant primitive references.

The full checkpoint's selected-lane reset falls from approximately 6.38 ms to 0.063 ms, about 101–102× for this control operation. This does not establish a 100× model speedup. The three-lane 71-step fidelity schedule retains 8,736 exact tokens and 522,240 exact audio samples against independent eager timelines using original resets, including 13 seconds of uninterrupted history before mixed tails/reuse. Same-shaped all-active timing isolates per-lane control overhead: about 0.05 ms encode and 0.10 ms decode in the final run, below 1% of per-push latency. Results include source hashes and explicitly repeated recordings/tokens.


## Exact cuBLASLt FP32 matrix layout research

Dense matrix multiplication remains the largest cost after the supported graph/fusion changes. The [cuBLAS 12.8 API documentation](https://docs.nvidia.com/cuda/archive/12.8.0/cublas/index.html) provides a useful route: cuBLASLt heuristic selection with `CUBLAS_COMPUTE_32F_PEDANTIC`, FP32 operands, and explicit row/column layouts. Legacy `CUBLAS_GEMM_ALGO0`–`23` selections have no effect on Ampere and newer hardware, so those IDs are not an effective tuning route on SM120. Pedantic FP32 does not itself guarantee the same reduction order as PyTorch; every proposed replacement is checked separately.

The research binding in `benchmarks/cublaslt.py` uses its own handle and the current CUDA stream, leaving PyTorch's handle/math mode untouched. It compares the original layout, an equivalent row layout, and a transposed contiguous FP32 weight copy. Tuning uses actual checkpoint weights/activations, separate warm and cache-evicted timings, FP64 error diagnostics, and eager/graph equality. A plan's buffers must outlive its captured graphs. Serial plans share a 32 MiB workspace, and operand alignment is validated.

One initial streaming attempt failed because its fresh heuristic shortlist omitted a previously measured descriptor. The binding now restores the measured opaque descriptor, requires the same cuBLASLt version, and calls `cublasLtMatmulAlgoCheck` for the operation/workspace. NVIDIA documents same-version descriptor serialization in the [algorithm type reference](https://docs.nvidia.com/cuda/archive/12.8.0/cublas/index.html#cublasltmatmulalgo-t). The experiment also rejects tuning from a different GPU name or PyTorch version. It does not substitute a different algorithm when the measured choice is absent from a new shortlist.

The two final sweeps record 1,118 component comparisons (with overlapping shapes). Several larger matrices retain bitwise equality while their selected transposed-layout algorithms improve warm/cold timings by roughly 1.3–1.8×. Smaller-row aggressive choices often change rounding; those are excluded. The selection rule requires exact captured output and graph replay, over 5% warm improvement, and over 10% cache-evicted improvement. Twenty-seven shape choices pass. Actual-weight stress checks add twelve activation variants per choice: recorded, negated, rescaled, zero, subnormal, tiny, large, sparse, and four random inputs. All 324 comparisons and graph replays are exact in `results/cublaslt_fidelity.json`.

Full-codec and long-stream gates are recorded in `results/full_cublaslt_experiment.json` and `results/full_cublaslt_streaming.json`. These preserve the original FP32 checkpoint and all quantizers. The offline corpus varies batch/chunk geometry while keeping 24 aggregate codec frames, and tests original-versus-packed eager and graph execution. The stream has eight lanes, 240 ms chunks, over ten seconds of uninterrupted history, independent pauses, tails, empty completion, and lane reuse. Benchmarks compare against the already optimized exact runtime, not the original eager baseline. Packed weights consume additional memory, and the implementation remains a research option under `benchmarks/`; the supported runtime and its approximately 5× batch-one table are unchanged.


## Single-copy resident FP32 weights

The initial packed-matrix experiment retained both original and transposed GPU weights. `benchmarks.packed_weights.PackedWeights` now shares a single packed tensor per Parameter across row-count plans. Its optional resident mode replaces the parameter's backing storage with a transposed view of that packed tensor. Parameter identity, logical dimensions, FP32 values, and arithmetic stay unchanged. On exit it reconstructs the original contiguous layout one parameter at a time after releasing the plans. There is no host offload or reduced-precision representation.

This uses the storage-sharing behavior of [PyTorch transpose](https://docs.pytorch.org/docs/main/generated/torch.transpose.html); a view changes dimensions/strides without another data allocation. A contiguous transpose makes the actual packed copy. Keeping a view back to the original dimensions allows the model to retain its logical weight values while cuBLASLt consumes the packed storage. Storage pointers and strides change during this research context, so old graphs and external aliases must not cross the transition. Frozen evaluation is required; overlapping matrix contexts and distinct Parameters sharing one storage are rejected. Unsupported matrix shapes reconstruct a temporary contiguous weight before ordinary linear execution. This preserves its original layout-dependent rounding, but can add copy overhead on those fallback calls. Autocast also follows that fallback path.

With the previous selected algorithms, the 14-case full-codec gate remains exact, including output checks after restoring the original layout. Peak allocated GPU memory falls from 13.25 to 7.45 GB while combined batch-eight latency stays approximately 26 ms. The long-stream gate remains exact with peak allocated memory falling from 11.64 to 8.74 GB. These are separate runs with identical workload geometry; they measure PyTorch allocated memory, not a claim about every allocator reservation or process-memory metric. The supported runtime defaults still do not enable experimental matrix replacement.

The broader batch sweep exposed a dispatch-specific fidelity issue that the flattened matrix stress gate could not detect: `encoder.7.output_proj` at batch 128 / one frame uses input shape `(128,1,1280)` and strides `(1280,1,1)`. It reports contiguous, but the leading strides do not satisfy PyTorch 2.8's `should_fold()` condition. The [pinned matmul source](https://github.com/pytorch/pytorch/blob/v2.8.0/aten/src/ATen/native/LinearAlgebra.cpp#L1783) therefore selects batched GEMM, whose rounding differs from flattening to 2-D GEMM. Tokens and audio were unchanged, but hidden states differed by up to 5.72e-6, so the initial batch gate was rejected (`results/full_cublaslt_batching_initial.json`).

A same-input layer audit returned reference outputs at each layer to prevent propagated errors and isolated that one projection across all three sources. The experimental wrapper now checks the pinned leading-stride folding rule in addition to contiguity, and preserves the native batched operation where folding would change dispatch. This applies to all matching layouts, not a layer-name exception. Tests exercise the fallback both before packing and after a different call has already packed the same weight. Matrix reports explicitly identify their flattened 2-D reference; future captures also retain native shape/stride metadata. The corrected layer audit and whole-codec rerun are retained alongside the rejected first attempt.


## Capability-based cuBLASLt search and matched confirmation

The next matrix experiment goes beyond the default heuristic shortlist using algorithm IDs, tile/stage capabilities, custom options, CTA swizzling, and split-K/reduction settings from the [cuBLAS API](https://docs.nvidia.com/cuda/archive/12.8.0/cublas/index.html). It initializes every candidate with pedantic FP32 compute and requires the capability flags for FP32 inputs, FP32 accumulators, and ordinary FMA. It retains all heuristic seeds and their split factors, including values outside the manually requested factors. Each operation descriptor passes `cublasLtMatmulAlgoCheck` before execution. This is a bounded search: one advertised custom range spans 262,144 values, so large ranges sample a prefix, individual bits, low-bit masks, and their maximum instead of claiming exhaustive enumeration.

The final search evaluates 12,422 valid configuration/layout/shape combinations on eight dominant actual-weight matrices at one, three, 24, and 384 rows. Only 350 are bitwise equal on the tuning activation. Finalists receive graph equality and separate warm/cache-evicted timing checks. Fifty-eight shape choices across this and prior reports pass the existing component selection rules, and all 696 actual-weight stress comparisons remain exact. Full-codec validation also remains exact on the 18-case real-audio batch sweep, including hidden states and restoration checks.

The whole-model outcome is a useful negative result. Alternating six rounds of previous/new resident configurations gives only **1.0072×** at batch one / one frame, **1.0004×** at batch eight / three frames, and **1.0026×** at batch 128 / three frames (`results/full_cublaslt_config_ablation.json`). These changes are below 1%, with overlapping timing variation in the first two cases. They do not justify replacing the existing default tuning or claiming another substantial model speedup.

A separate bracketing check (`cublaslt_confirm.py`) remeasures the baseline before and after each finalist. It exposes an unrepresentative initial baseline in the long search: the first row-one expansion matrix measured 65.18 microseconds initially versus roughly 34.12 microseconds during fresh confirmation. Thus its apparent roughly 6× warm ratio is not reliable; the repeated matched component result is roughly **3.1× warm**, and the selected custom option reaches only about **1.1×** after the eviction protocol. The actual model comparison remains decisive. Other warm winners can regress after eviction, and the three-row FFN shapes produce no useful exact improvement. Raw screening, confirmation, and full-model ablation reports are kept separately to avoid conflating them.

An algorithm reporting zero numerical capability flags was excluded from execution; a separate default-configuration support probe for ID 76 returned `CUBLAS_STATUS_NOT_SUPPORTED` in six tested shape/layout combinations (`results/cublaslt_unknown_capability.json`). This does not claim that every possible configuration of that ID is unavailable. Further work should change the optimization approach for the dominant matrix cost, rather than repeatedly searching the same vendor configuration space or treating warm-cache ratios as codec gains.


## Component-product tensor cores and a discrete fidelity failure

After the vendor search showed little remaining benefit, two Triton tensor-core prototypes were implemented with FP32 checkpoint storage and FP32 output. `tf32x3` uses Triton's three-product correction path. A separate manually implemented `bf16x6` path decomposes each operand into three BF16 components, evaluates six selected component products, keeps a separate correction accumulator, and adds the high-product block sums with explicit CUDA `add.rn.f32`. It is not the cuBLAS BF16x9 emulation feature, and does not require that feature's hardware support. Neither prototype changes the learned checkpoint, distills the model, or uses FP8/FP4.

Research motivation comes from [Ootomo and Yokota's single-precision tensor-core accuracy work](https://arxiv.org/abs/2203.03341) and its [quantum-circuit application](https://arxiv.org/abs/2303.08989), which discuss component corrections, accumulator rounding, and exponent range. Our kernels are experiments, not a port of their implementation or a reproduction of their A100 performance claims. [Triton's dot documentation](https://triton-lang.org/main/python-api/generated/triton.language.dot.html) describes the multi-pass precision options and the rounding caveat for ordinary TF32.

`tensorcore_shapes.py` measures original-weight matrices against FP64 diagnostics and exact PyTorch results, while separating warm/cache-evicted timing and eager/graph equality. Initial tiles have high register use; reducing tile sizes yields spill-free variants with 71 registers in a representative compact configuration. Larger dense matrices remain slower than vendor FP32 in these implementations. BF16x6 is closer to FP64 in some cases and farther away in others. None of the changed matrix paths is bitwise equivalent to the reference. This is evidence about the implemented kernels on SM120, not a claim that every possible tensor-core implementation must lose.

Direct paired comparison with the already tuned exact matrices (`tensorcore_select.py`) narrows the plausible case to a short QKV projection: `(M,N,K)=(24,3840,1280)`. TF32x3 is about 1.43× faster with warm weights, but only 0.87× as fast under the eviction protocol. A deliberately limited full-codec probe applies it to that QKV shape and leaves every other operation on the previous exact resident path. Combined batch-eight latency changes from approximately 26.015 to 25.392 ms, about 2.4%, but the candidate fails fidelity.

The 14-case probe changes **24 of 10,752 tokens** in one speech case. Maximum encoder hidden-state error there is only **8.58e-6**, yet maximum round-trip waveform error becomes **0.1522**, with RMSE **0.00646** and signal-to-error ratio **21.82 dB**. Holding original codes fixed reduces the decoder-only maximum difference to **1.23e-5**. Eager and graph outputs agree, so this is an arithmetic/quantization effect, not graph capture drift. `results/full_tensorcore_probe.json` records each changed quantizer/lane/frame coordinate and separates fixed-code decoding from the round trip.

`tensorcore_tie.py` traces the first changed decision using the full batch's original FP32 distance calculation. At zero-based quantizer 5, lane 6, frame 2, code IDs 198 and 1007 have reference squared distances **0.2859185934** and **0.2859187126**, a gap of **1.1920929e-7**. The small latent perturbation reverses their order. Further residual quantizers then produce additional changed tokens. These observations show why small matrix/latent RMSE and exact tokens on other clips do not establish quality preservation.

The candidate remains outside the runtime. Its failing eight-lane speech input is now part of `benchmarks.fidelity` as `speech_near_tie_b8_f3`, with source checksum and lane offsets recorded. Both supported residual backends pass the expanded 13-case corpus exactly. Future encoder arithmetic changes must face this regression alongside broader audio coverage; an approximate encoder would need a defensible strategy for discrete decisions, not just a small average-error claim.


## Stable-slot request scheduling and fused input gather

The existing masked streaming session could preserve independent lanes, but callers had to assign requests and refill slots themselves. NVIDIA's [sequence scheduling documentation](https://docs.nvidia.com/deeplearning/triton-inference-server/user-guide/docs/user_guide/implicit_state_management.html#scheduling-strategies) explains why a stateful model with per-slot caches keeps each sequence in a dedicated slot and reassigns that slot after completion. `StreamingBatcher` implements this principle locally with FIFO admission, without adding an inference-server dependency. Active requests retain their slot, fixed batch shape, graph, and KV addresses. Finished or cancelled slots reset before reuse.

A Triton gather reads a small GPU table of source pointers, channel strides, offsets, and valid lengths. One launch copies the next input chunk from all assigned requests and zero-fills inactive lanes and tails; masked loads never dereference absent requests. The scheduler owns contiguous input copies on its construction stream, retaining them through the gather. It releases completed input storage and returns separately owned output chunks. Host metadata drives scheduling, so per-step output lengths do not require a device-to-host synchronization. Existing preparation kernels still handle effective encoder lengths and persistent completion state.

The isolated gather benchmark covers batch sizes 8/32/128 for audio and codes. All six cases are exact against zero-fill plus per-lane PyTorch copies. At batch eight, graph-amortized packing falls from **5.75 to 0.85 μs** for audio and **5.11 to 0.86 μs** for codes. At batch 128, ratios reach roughly 29×/25×. This component measurement has resident metadata and excludes its upload; it does not establish those gains for the model. The full-queue benchmark includes submission copies, metadata preparation, gather, resets, output ownership, and concatenation.

A heterogeneous queue of 16 requests uses frame counts `[129,3,6,9,12,15,18,21]` twice, with selected final audio tails shortened by 17 samples. Inputs cycle through the three hashed real-audio sources and are repeated explicitly where needed. The two longest requests each exceed ten seconds. Compared with admitting a new group of eight only after the previous group finishes, immediate refill reduces model steps from **86 to 44**, for the same **142 active chunk slots**. Useful slot occupancy rises from **20.64% to 40.34%**; the remaining tail inefficiency is visible rather than hidden by a peak-throughput claim.

Three alternating paired rounds on the full checkpoint yield median queue times **1837.35 → 941.75 ms encode** and **1669.98 → 857.26 ms decode**, about **1.951×/1.948×** over the same optimized runtime at the same batch size. All **13,632 code tokens** and **817,920 decoded samples** match original eager independent timelines at the same batch shape, with unchanged graph identity. A separate CuTe residual run is also exact and measures approximately 1.95× in both directions. There is no experimental tensor-core or matrix substitution in these runs. This is a scheduling improvement for the measured uneven queue, not an additional multiplier for the batch-one table. Equal-length queues can have no refill benefit.

The API accepts finite CUDA input tensors and permits new submissions between steps; it does not yet accept incremental input fragments or provide network serving. Queue capacity bounds request count, not bytes. Callers retain source audio lengths for final waveform trimming. Scheduling keeps a fixed batch size because changing it can change vendor arithmetic; the independent reference deliberately uses the same batch geometry. Wider input coverage, incremental request admission, cross-device execution, and reducing dense FP32 matrix cost remain necessary work toward the full objective.


## Exact LFQ selection, residual updates, and unused encoder outputs

The quantizer still launched many small operations after the earlier cache optimizations. `quantizer_backend="triton"` keeps the original normalization, FP32 vendor GEMM, codebooks, and all requested quantizers, while fusing the arithmetic around them. A row kernel computes `(row_norm - dots) + codebook_norm`, selects the index, gathers the original embedding, and optionally evaluates `latent + (embedding - latent)`. FP contraction is disabled. Both distance roundings and both straight-through roundings remain necessary: removing a common row norm can change a rounded tie, and simply returning the embedding can change cancellation results.

[Torch's maximum documentation](https://docs.pytorch.org/docs/main/generated/torch.max.html) specifies the first matching index for ties. Its [pinned reduction implementation](https://github.com/pytorch/pytorch/blob/v2.8.0/aten/src/ATen/native/SharedReduceOps.h) additionally prioritizes the first NaN. [Triton's argmin documentation](https://triton-lang.org/main/python-api/generated/triton.language.argmin.html) only promises its leftmost rule for non-NaN values, so the kernel explicitly reduces NaN indices separately. Tests include equal-distance codes, a rounding tie that would reverse if the row norm were dropped, infinities, and first-NaN selection.

The initial numerical kernel passed but a stride assertion failed for a gapped singleton-time input: the upstream addition produced `(8,1,8)` while the candidate allocated `(8,1,1)`. Although values were identical, layout-dependent downstream dispatch must not be assumed harmless. Noncanonical latent strides now use the original straight-through additions after fused selection; canonical convolution outputs retain the fused path. Raw embedding outputs preserve the original B,T,D allocation followed by transpose.

A second kernel combines the masked residual subtraction, next-step masking, and public quantized-vector accumulation. For the pinned encoder caller, the accumulated vectors are never returned or consumed: only code indices, lengths, and pre-quantizer hidden states are exposed. That path omits vector accumulation, its output projection, and the last quantizer's unused projected vector. It still computes the final code from the unchanged distance operation. Direct quantizer calls preserve all three original outputs. Local or global forward hooks disable the encoder-only omission so observers can still inspect those values. A temporary flag is restored even on exceptions; CUDA graphs capture the selected path normally.

Three alternating paired rounds against the previous optimized runtime give:

| Batch / frames | Public quantizer, previous → fused | Encoder, previous → fused | Encoder latency reduction |
| --- | ---: | ---: | ---: |
| 1 / 1 | 1.116 → 0.772 ms | 7.875 → 7.498 ms | 4.79% |
| 1 / 3 | 1.530 → 1.078 ms | 9.488 → 9.012 ms | 5.01% |
| 8 / 3 | 1.596 → 1.109 ms | 16.406 → 15.805 ms | 3.66% |
| 128 / 3 | 2.426 → 1.836 ms | 86.502 → 85.843 ms | 0.76% |
| 8 / 40 | 2.150 → 1.624 ms | 71.655 → 71.088 ms | 0.79% |

The public quantizer still returns its vectors, so its timing is distinct from the encoder-only omission. All measured quantized vectors, indices, lengths, and encoder codes remain exact. The preliminary selector-only report is retained separately; its first 1×1 encoder baseline was much slower than later matched baselines, so that initial ratio is not a model performance claim. The final three-round ablation is authoritative.

Both Triton and CuTe residual configurations pass the 13-case full-model corpus with quantizer fusion, including the speech decision separated by only 1.19e-7. The 16-request long queue also remains exact against original eager timelines: 13,632 tokens and 817,920 decoded samples, with lane reuse and two requests beyond ten seconds. These are tested-input guarantees, not a universal proof for arbitrary data or changed libraries.

Paired full-model profiles show **2,054 → 1,727 encoder kernels per replay**, a reduction of 327, including 32 fused selectors and 31 residual-update kernels. Decoder execution stays at **1,182 kernels**. Dense FP32 SGEMM-named kernels remain **77.10%** of encoder and **80.04%** of decoder GPU kernel time in the new profile. This confirms the next priority: the dominant matrix cost remains, despite useful exact fusion gains. The supported fusion is opt-in and does not establish 100× acceleration.


## Lossless FP32 weight storage and transparent hardware compression

With dense matrix kernels still taking roughly 77–80% of device time, the next experiment targets weight traffic without changing arithmetic. [DFloat11](https://arxiv.org/abs/2504.11651) motivates exponent-entropy coding but studies primarily BF16 storage and reports its largest throughput gains against CPU offload. Those ratios do not apply to this fully GPU-resident FP32 codec. [ZipServ](https://arxiv.org/abs/2603.17435) emphasizes eliminating intermediate decompression buffers with fused decompression/GEMM; its tensor-core results likewise do not reproduce the vendor FP32 reduction used here. These papers motivate the investigation, not its performance claims.

`benchmarks.lossless_weights` implements a simple integer bitstream: each block retains all 24 sign/mantissa bits per value, stores the minimum exponent, and packs the exact exponent deltas using zero to eight bits. Per-block word offsets permit parallel decoding. The format reconstructs all 32 original bits, including signed zero, subnormal values, infinities, and NaN payloads. It never converts model weights to another floating-point precision. Preprocessing performs a synchronized prefix-sum size read and is intentionally outside graph capture. Reconstruction can write into reusable FP32 matrix storage for the original vendor `F.linear` call.

The 48 actual-weight variants cover three sampled FFN/QKV weight matrices at 12 `(M,N,K)` geometries (rows 1/3/24/384) and block sizes 128/256/512/1024. Every restored weight and eager/graph GEMM output is exact. Storage ratios are **1.128–1.138×**, or roughly **11.3–12.1% fewer bytes**, including metadata. Nevertheless, reconstruction plus GEMM gives only **0.400–0.804×** cache-evicted performance relative to ordinary GEMM: approximately **1.24–2.50× slower**. Separate copy-plus-GEMM timings and fresh final baselines are retained. These results reject this separate reconstruction pipeline for runtime promotion; they do not rule out a future fused kernel that also preserves the required arithmetic order. The ratio is for the encoded representation, not measured whole-model memory savings.

The initial kernel requested an invalid combination of `.cg` and `.evict_first`; the installed assembler rejected it. Corrected loads use a supported eviction hint. No accepted results come from the rejected assembly. This is a cache hint, not evidence that reconstructed weights remain in L2.

A separate driver probe reports **48 MiB L2** and `CU_DEVICE_ATTRIBUTE_GENERIC_COMPRESSION_SUPPORTED=1` on this RTX 5070 Ti. This capability differs from nvCOMP's dedicated Blackwell decompression engine: NVIDIA's [DE documentation](https://docs.nvidia.com/cuda/nvcomp/decompression_engine_faq.html) currently lists B200/B300/GB200/GB300, not this device. The [CUDA allocation-property reference](https://docs.nvidia.com/cuda/archive/13.0.0/cuda-driver-api/structCUmemAllocationProp__v1.html) requires checking the returned allocation properties because requesting compression does not guarantee that the driver grants it.

`benchmarks.compressible_memory` uses CUDA VMM to allocate, map, and grant access to memory, checks the actual compression flag, and exposes FP32 storage through the CUDA array interface. Tensor storage keeps the allocation owner alive; final release synchronizes before unmapping/freeing. Tests cover ordinary/compressible allocations, exact arbitrary bit patterns, graph use, and owner lifetime. External tensors must remain alive through graph replay. This research allocator does not claim a smaller virtual allocation, and its memory is outside PyTorch's caching-allocator accounting.

The matched hardware comparison uses ordinary PyTorch memory, uncompressed VMM memory, and verified compressible VMM memory in three alternating rounds per geometry. All **126 matrix comparisons** across twelve actual-weight cases plus two controls are exact. The sampled checkpoint matrices show cache-evicted ratios of **0.955–1.008×** against the matched uncompressed VMM allocation, with no material benefit. A zero-matrix control improves **2.2×**, demonstrating data-dependent hardware benefit; a BF16-roundtrip matrix used solely as another control has no cold gain. Neither control is used as a model replacement. An apparent 1.05× result versus PyTorch memory is shared by uncompressed VMM and cannot be attributed to compression.

Both approaches remain outside the supported runtime. The evidence favors eliminating decompression/materialization traffic in any future compression-based matrix kernel, and shows that simply changing allocation flags does not speed up these dense FP32 weights. Existing exact runtime behavior and the 100× objective are unchanged.


## Incremental input scheduling and direct fragment gathering

NVIDIA's [stateful execution documentation](https://docs.nvidia.com/deeplearning/triton-inference-server/user-guide/docs/user_guide/model_execution.html) describes per-lane ready/end controls. Its [direct scheduling strategy](https://docs.nvidia.com/deeplearning/triton-inference-server/user-guide/docs/user_guide/implicit_state_management.html) retains each stateful sequence in a dedicated slot. These provide useful design precedents for the existing MOSS streaming state, which is already stored per lane. Our application is a local Python scheduler with Triton-language GPU kernels; it does not integrate Triton Inference Server or implement its serving protocol.

`StreamingBatcher` now accepts incremental input. Nonfinal requests wait until one complete configured chunk is available; explicit final input releases a short tail. Ready queued requests can bypass underfilled queued requests before lane assignment. An active request waiting for more data keeps its lane and causal history, while other lanes continue. When all active requests wait, `step()` does not run the model. A late final notification after the last full chunk emits an empty final result and frees the lane; reset occurs before its next occupant. The fixed batch geometry preserves vendor dispatch, even when some lanes are inactive.

Each append owns a contiguous fragment. A segmented Triton gather packs valid spans and zero padding in one launch without concatenating fragments first. Host-built descriptors identify source pointers, source/destination offsets, lengths, and lanes; separate tile tasks avoid scanning every fragment in every output tile. The descriptor count is a runtime argument, avoiding compilation for each new fragment count. Complete contiguous chunks retain the original gather fast path. Optional byte capacity counts retained tensor payloads, including consumed prefixes of partially held fragments; it does not bound allocator reservation, model state, or outputs. The request-count bound remains independent.

`results/full_incremental_batching.json` checks ten full-checkpoint requests at batch eight / three frames against independent original eager timelines. The audio comes from the three hashed recording sources, repeated cyclically with documented offsets; two requests contain 129 codec frames and exceed ten seconds. All **11,072 code indices and 664,320 decoded samples** are exact in every paired round. Uneven arrivals exercise paused lanes, queue bypass, late completion, tails, and reuse, with one captured graph per direction. Nine requests emit output before their last input arrives. Arrival ticks are logical host-loop iterations without real-time waits; this is evidence of incremental behavior, not network latency.

Three alternating timing rounds compare one fragment per arrival with three nonempty fragments when the arrival is long enough. The complete schedules are warmed before measurement. Encoder medians are **980.417 / 982.021 ms**; decoder medians **933.580 / 934.928 ms**, adding approximately **0.16% / 0.14%** for fragmentation. Both sides include input clones, metadata transfer, packing, resets, owned outputs, and concatenation. The preliminary run warmed only short prefixes and included new gather compilations in its first fragmented round; those timings are superseded by the complete warmup and runtime descriptor-count implementation. A separate CuTe residual run is also exact (`results/full_incremental_batching_cute.json`).

The isolated host-driven packing benchmark (`results/fragment_gather.json`) includes allocation and metadata upload, comparing the fused gather with zero-initialization, per-lane concatenation, and copies. All six batch/direction cases are exact. At batch eight, audio packing falls **0.166 → 0.117 ms** (1.42×), and code packing **0.163 → 0.091 ms** (1.80×). At batch 128, component ratios reach 2.12× and 5.49×. These are component wall times against the stated straightforward baseline, not GPU-only timings or model multipliers. Dense FP32 matrices remain the primary execution cost, and the 100× whole-model target remains unmet.


## Supported resident matrix plans and graph/storage lifetimes

The measured cuBLASLt choices now have an opt-in runtime path through `optimized(..., matrix_backend="cublaslt")`. NVIDIA's [cuBLAS reproducibility documentation](https://docs.nvidia.com/cuda/cublas/#results-reproducibility) limits bitwise reproducibility across library versions and device configurations, and discusses explicit workspace ownership for concurrent streams. This motivates exact environment checks and separate workspaces; it does not establish equality between different algorithms or with PyTorch. Our selection still depends on the recorded component stress tests and full-model comparisons.

The bundled profile retains 57 packed matrix choices over 16 weight geometries from the existing tuning reports. It requires the pinned MOSS revision, RTX 5070 Ti (SM120, 70 SMs), PyTorch 2.8.0+cu128, and cuBLASLt 12.8.4. Plans explicitly use `CUBLAS_COMPUTE_32F_PEDANTIC`, FP32 operands/outputs, and the validated serialized algorithm descriptor. No new approximate arithmetic or weight precision is introduced. Unprofiled shapes, bias, unsupported strides/alignment, gradients, and autocast retain the original contiguous-weight path; the singleton-stride guard remains necessary to preserve PyTorch's matrix/batched-matrix dispatch.

Packing every eligible weight on context entry was tested and rejected. Although exact, it made batch-one / three-frame execution approximately 43.4 ms instead of 17.0 ms, because unselected operations had to transpose resident weights back on every replay. `results/full_matrix_runtime_prepack.json` preserves that failed approach. The final runtime packs only weights actually reached by a validated shape, before capture during ordinary execution or graph warmup. Parameters keep their identity and all FP32 values, while storage and strides change until restoration. Registered parameter/buffer aliases and tied parameter names are rejected before any transition. External aliases remain a documented caller constraint.

Storage transitions synchronize the device and advance a device-wide epoch. `GraphedCallable` rejects a stale epoch before submitting work, including graphs captured before entry or before another weight was packed. Warmup can pack weights before the graph takes its final storage snapshot; opening/closing a matrix context inside the captured function is rejected. This intentionally invalidates unrelated managed graphs on the same device too. Reusable collections should warm all needed shapes before capture. Raw CUDA graphs are outside this guard and must not cross transitions. Each used stream has independent plans and a 32 MiB workspace; runtime calls are restricted to one host thread. Exit synchronizes, closes plans, and restores one weight at a time. The supported binding is shared with research tools through compatibility imports.

`results/full_matrix_runtime.json` records three alternating paired rounds at four batch/frame geometries, with quantizer fusion enabled on both sides. Every round compares original eager codes, hidden states, and audio from three real recording sources, as well as model outputs after restoration. All are exact. Combined encode/decode medians are:

| Batch / frames | Previous optimized | Supported matrices | Additional gain |
| --- | ---: | ---: | ---: |
| 1 / 1 | 14.291 ms | 14.293 ms | 1.000× |
| 1 / 3 | 16.967 ms | 16.200 ms | 1.047× |
| 8 / 3 | 30.395 ms | 25.501 ms | 1.192× |
| 128 / 3 | 169.851 ms | 146.823 ms | 1.157× |

Peak allocated memory stays below 7.8 GB across these runs. The batch-one / one-frame case constructs no matrix plans and is effectively unchanged. The matrix option remains opt-in; mixed-shape sequences can incur fallback copies after earlier packing and should not inherit these fixed-geometry gains without measurement. These are warmed graph times, including graph input/output ownership; model loading, packing, plan construction, capture, and restoration are outside the timing loop. Setup and memory records remain separate.

Both `full_fidelity_matrix_runtime.json` and `full_fidelity_matrix_runtime_cute.json` pass all 13 corpus cases, including the near-tie speech regression. `full_incremental_matrix_runtime.json` also retains exact 11,072 tokens and 664,320 samples across ten uneven requests, two over ten seconds, at batch eight / three frames. Its one-fragment/three-fragment times are 862.576/864.235 ms encode and 812.515/817.501 ms decode. This is a streaming fidelity gate with logical arrival ticks, not a fresh paired matrix ablation or a network latency claim.

The new batch-eight profile (`results/full_matrix_runtime_profile.json`) still attributes **77.78% encoder / 79.93% decoder** device kernel time to SGEMM-named kernels, with attention at **6.99% / 8.38%**. Kernel-name groups exclude host time and memcpy and are not a complete semantic operator attribution. The profile confirms that dense FP32 matrix execution still dominates. The accepted change makes the earlier measured matrix gain available through the supported API with explicit lifetime checks; it does not demonstrate the requested 100× whole-model speedup.


## Exact LFQ output projections and projected decoder codebooks

The supported matrix profile leaves repeated small quantizer convolutions and their surrounding operations. NVIDIA's [convolution performance guide](https://docs.nvidia.com/deeplearning/performance/dl-performance-convolutional/index.html) describes their implicit-GEMM interpretation and the importance of the channel reduction dimension. Mathematical equivalence to a matrix multiply does not imply the same FP32 rounding order; the probes test that distinction directly.

`benchmarks.pointwise_projection` compares forward/reverse fused multiply-add, split accumulators, separate multiplication/addition, and a `linear()` substitution on actual checkpoint projection weights. For the first eight-channel output weight, forward-order FMA followed by a separate bias addition is exact at all five tested geometries; the alternatives change rounding at smaller shapes. Its resident component ratios are approximately 2.13–5.99× versus native convolution. The analogous 512-channel input projection changes values at batch one / one frame; at the other tested shapes it is exact but slower. Input projections remain native. These probes use one learned weight each and synthetic activations, not whole-model quality evidence.

`fast_moss.projections` implements the accepted eight-FMA sequence and separate bias rounding. In the encoder's unobserved codes-only path, a second kernel combines projection, mask multiplication, residual subtraction, and next-step masking, retaining every rounding boundary. The actual straight-through latent is still projected: it can differ from the raw codebook embedding through cancellation, so encoder residuals cannot simply reuse a projected decoder lookup. Observed/public paths retain the projection call. The initial component test caught a noncanonical singleton-time output stride; that case now uses native convolution.

Decoder embeddings are fixed per code. The new `projection_backend="triton"` option therefore computes their original output convolution once, using native cuDNN with cached FP32 weights, and retains a **32×1024×512 FP32 table (64 MiB)**. A gather adds selected vectors in the original codebook order, followed by the unchanged final output projection. No weights or embeddings are quantized, no codebook is omitted, and the encoder nearest-code calculation is unchanged. Code bounds retain asynchronous CUDA assertion behavior with bounded table loads; extra codebooks beyond the original model count remain ignored. Autocast and observer hooks retain native paths. The option requires the original LFQ geometry, cached weights, RTX 5070 Ti / 70 SMs, PyTorch 2.8.0+cu128, and cuDNN 9.10.2, with cuDNN enabled and its TF32/benchmark flags disabled.

The cache lifetime synchronizes device work on entry/exit and advances the managed-graph resource epoch. This conservatively invalidates all managed graphs on the device, including unrelated ones, and prevents replay after the table is freed. Raw CUDA graphs remain subject to the documented lifetime constraint. Context exit restores methods and cached attributes, including after partial setup failure. Cache setup uses functional convolution calls so it does not manufacture projection observer events.

`results/projection_components.json` contains **864 exact comparisons**: all 32 learned output weights, five activation geometries, four scales plus graph replay, and two comparisons covering every codebook entry across projection layouts. `results/full_fidelity_projections.json` and `full_fidelity_projections_cute.json` pass all **13 full-model corpus cases each**, including the near-tie speech regression, with matrix and quantizer optimizations enabled. `results/full_incremental_projections.json` retains **11,072 tokens and 664,320 decoded samples** exactly across pauses, late completion, tails, lane reuse, and two requests longer than ten seconds. These remain tested-input guarantees, not proofs for changed libraries or arbitrary model modifications.

The first decoder gather wrote output-contiguous tiles but scattered its table reads across tokens. A token-oriented tile instead reads contiguous channel vectors and scatters only the final output stores. In `results/projected_gather.json`, all twelve mode/geometry cases remain exact; token tiles improve the tested multi-frame components approximately **1.2–9.0×**, while the flat variant is retained at one frame. The benchmark uses a resident random table and excludes validation, allocation, the final output projection, and transformer work. `results/full_projection_runtime_flat.json` retains the preliminary flat-gather model ablation; final claims use the subsequent complete ablation.

Three alternating paired rounds in `results/full_projection_runtime.json` compare against the supported matrix/quantizer runtime:

| Batch / frames | Encoder, previous → new | Decoder, previous → new | Decoder quantizer, previous → new |
| --- | ---: | ---: | ---: |
| 1 / 1 | 7.499 → 7.426 ms | 6.744 → 6.606 ms | 0.179 → 0.057 ms |
| 1 / 3 | 8.694 → 8.571 ms | 7.381 → 7.159 ms | 0.253 → 0.059 ms |
| 8 / 3 | 13.358 → 13.075 ms | 11.923 → 11.618 ms | 0.243 → 0.061 ms |
| 128 / 3 | 74.261 → 73.945 ms | 72.377 → 71.883 ms | 0.689 → 0.112 ms |
| 8 / 40 | 71.220 → 70.968 ms | 68.367 → 67.977 ms | 0.496 → 0.105 ms |

All eager and graph outputs in every round are exact against original eager references. Quantizer reconstruction improves **3.14–6.16×**, while whole-model gains are much smaller: roughly 1–3% at the smaller geometries and below 1% at the larger ones. The graph timings include input copies and owned outputs and exclude loading, cache construction, packing, capture, and restoration. The cache size is a tensor payload measurement; it is not inferred from a whole-model peak-memory difference.

The final batch-eight profile (`results/full_projection_profile.json`) shows **1856 → 1763 encoder kernels** and **1278 → 1091 decoder kernels** against the previous matrix profile. It records 31 fused projection/update calls in the encoder and one projected-table gather in the decoder. SGEMM-named kernels still account for **77.67% / 80.93%** of encoder/decoder device kernel time. Dense FP32 matrix execution remains the main unresolved cost; these exact local gains do not establish 100× whole-model acceleration.


## Ordered FP32 SIMT matrices and singleton residual layout

The preceding profile still attributed roughly 78–81% of device kernel time to SGEMM-named operations. Instead of repeating the vendor configuration search, this experiment reconstructs their accumulation structure in Triton. NVIDIA's [matrix multiplication guide](https://docs.nvidia.com/deeplearning/performance/dl-performance-matrix-multiplication/index.html) explains the limited arithmetic intensity of small-row matrices. The [cuBLAS algorithm configuration API](https://docs.nvidia.com/cuda/cublas/#cublasltmatmulalgoconfigattributes-t) exposes split counts and reduction schemes. CUTLASS's [SIMT thread implementation](https://raw.githubusercontent.com/NVIDIA/cutlass/v3.8.0/include/cutlass/gemm/thread/mma_sm50.h) and [split-K kernel](https://raw.githubusercontent.com/NVIDIA/cutlass/v3.8.0/include/cutlass/gemm/kernel/gemm.h) motivate sequential FMA and aligned partitions. These sources do not promise equality with a particular vendor binary; the measured arithmetic probes determine the accepted boundaries.

`benchmarks.ordered_mm` tests consecutive FP32 FMA partitions and an ordered sum of their partial results on actual checkpoint FFN weights. For `(M,N,K)=(24,5120,1280)` and `(24,1280,5120)`, a **256-term partition** matches the original flattened `F.linear` output. Other tested boundaries differ. None of the tested boundaries reproduce the two three-row cases; this bounded probe does not rule out a different three-row implementation. The scalar-load prototype is roughly six times slower than the selected vendor kernels. A first tiled attempt requested dimensions below Triton's supported dot minimum and failed compilation; the corrected tile sweep contains 24 exact shape/configuration comparisons.

A 32×128 output tile and 32-term load tile allow Triton `tl.dot(input_precision="ieee")` to reuse activations while retaining the measured partition order. The second kernel adds partials sequentially with FP contraction disabled. `results/ordered_confirm.json` contains **432 exact eager/graph stress comparisons**, across the selected vendor plan, seven research tiles, and the integrated runtime, using actual/negated/scaled/zero/subnormal/tiny/large/sparse inputs and four random variants for each learned weight. The final runtime achieves approximately **1.47× / 1.35× warm component speedups**, but approximately **0.957× / 1.000×** after the cache-eviction protocol. The compiler report separately confirms FP32 FMA, no matrix Tensor Core instructions, and no spills. These are component results, not whole-codec multipliers.

The option `matrix_backend="triton"` uses these two ordered FFN shapes and retains the existing cuBLASLt/native paths elsewhere. It requires the pinned model/GPU/library profile and Triton 3.4.0. The same MatrixRuntime owns lazy packed parameters, separate per-stream plans/workspaces, one-host-thread enforcement, and graph epochs; the Triton path consumes the existing packed storage. Context exit restores weights and methods. Hooks remain on the module call path, and unsupported strides, row counts, alignment, autocast and other existing fallbacks retain native behavior. Both backends remain opt-in. A specialized launch that removed bounds/index structure was exact but slightly slower in component measurements; the final runtime preserves the measured tiled launch. Its earlier reports remain separately labeled `*_specialized.json`.

The broader full-model check exposed an existing residual-layout defect. At `encoder.7.transformer.layers.0`, batch 24 / one frame has a contiguous input with strides `(1280,1,1)`. `empty_like` preserved those strides, whereas native residual addition returned `(1280,1280,1)`. The residual values themselves were equal, but a downstream linear selected different arithmetic. Encoder hidden states differed by up to **5.72e-6**, while tokens/audio remained exact. The failure also occurs with the prior cuBLASLt backend and with matrix optimization disabled; disabling residual fusion removes it. `results/ordered_singleton_audit.json` records this isolation, and `residual_stride_audit.json` returns native outputs at each same-input residual to prevent propagated differences.

The pinned [PyTorch TensorIterator implementation](https://raw.githubusercontent.com/pytorch/pytorch/v2.8.0/aten/src/ATen/TensorIterator.cpp) allocates canonical contiguous output for its contiguous fast setup. Both Triton and CuTe residual wrappers now do the same. Ten new residual tests check values, output strides, downstream `F.linear`, and graph replay across singleton layouts. The standard full-model corpus now includes batch 24 and 128 singleton-frame speech inputs. This fixes a real hidden-state fidelity failure; it is not a relaxation of the gate.

`results/full_ordered_runtime.json` passes **27 full-checkpoint cases** against original eager codes, hidden states and waveforms, including graph replay and restoration. It extends the standard corpus with all three recordings at four 24-row batch/frame layouts. Three alternating context-by-context timing rounds show small, variable effects: from a **1.4% decoder regression** to a **2.6% encoder gain**. The first failed report is retained as `full_ordered_model_initial.json`; the corrected research prototype is recorded separately from the final integrated runtime.

A tighter measurement holds one packed-weight lifetime and constructs two graphs per direction, then alternates their execution order for **40 pairs**. Both graphs pass original-reference equality. `results/full_ordered_paired.json` reports:

| Batch / frames | Encoder, cuBLASLt → ordered | Decoder, cuBLASLt → ordered | Candidate-faster pairs, encoder / decoder |
| --- | ---: | ---: | ---: |
| 8 / 3 | 13.224 → 12.920 ms | 11.593 → 11.506 ms | 27/40 / 26/40 |
| 24 / 1 | 14.079 → 13.783 ms | 12.651 → 12.546 ms | 24/40 / 24/40 |
| 1 / 24 | 13.078 → 12.779 ms | 11.413 → 11.338 ms | 26/40 / 25/40 |

The distributions overlap. Medians of paired savings are only **0.116–0.180 ms encode** and **0.067–0.097 ms decode**, which differ from subtracting the two independent medians. The run includes graph input copies and owned outputs, excluding loading, packing, capture and restoration; holding two graphs is explicitly part of this measurement protocol. Peak allocated memory is **7.60 GB** here and **7.70 GB** in the context-by-context full-model report. No broad latency guarantee or 100× claim follows from these modest results.

The independent CuTe run passes the expanded **15-case** corpus. Incremental batch-eight streaming retains **11,072 tokens and 664,320 samples** exactly across pauses, tails, late completion and lane reuse, including two requests longer than ten seconds. One-fragment/three-fragment schedule times are **850.894/853.428 ms encode** and **796.643/801.382 ms decode**; this is not a paired matrix-backend timing comparison or network-serving result.

The final profile counts **64 ordered main loops and 64 ordered reductions per direction**, replacing 64 FFN matrices. Total kernels rise by 32 per direction to **1795 encode / 1123 decode** because the new path always has a separate reduction. Vendor SGEMM, ordered matrices, and vendor split reductions together consume approximately **80.44% / 83.93%** of kernel time. The apparent fall in the SGEMM-named group alone is a change in kernel names, not elimination of matrix work. Further work should reduce actual weight traffic and matrix/reduction costs; the full 100× objective remains unmet.


## FFN pipeline depth and exact decoder epilogues

The previous profile leaves dense FP32 work dominant, so this pass varies pipeline depth, tile width, loop unrolling and warp count while retaining the original 256-term accumulation partitions. Triton's [range documentation](https://triton-lang.org/main/python-api/generated/triton.language.range.html) explains pipeline and unroll controls; the experiment uses the installed 3.4.0 implementation. `results/ordered_pipeline.json` records 66 trials, including two configurations exceeding the device's shared-memory limit. All 64 executed trials are exact. Aggressive unrolling can spill registers and lose performance. The first cold baseline is an outlier and is not used to claim a large speedup.

`results/ffn_kernel_resources.json` inspects the actual runtime kernels: moving from three stages to two halves shared memory from **40,960 to 20,480 bytes**, with **166 registers and zero spills unchanged**. Both retain FP32 FMA PTX and no matrix Tensor Core instructions. The exploratory generic pipeline kernel's lower register count does not describe the accepted runtime. Fused GELU/residual reductions use 18/40 registers, zero shared memory and zero spills. Actual-input matrix and fused results are bitwise equal in all four resource checks.

Fusion initially fails exact GELU: `results/ordered_epilogue.json` retains **20,025 differing elements out of 122,880**, maximum 4.77e-7, while the residual epilogue is exact. PyTorch's [pinned GELU source](https://raw.githubusercontent.com/pytorch/pytorch/v2.8.0/aten/src/ATen/native/cuda/ActivationGeluKernel.cu) evaluates FP32 multiplications around `erf`. Explicit outer rounding alone is insufficient. `results/gelu_math.json` isolates the math library using 2,097,152 identical ramp/random inputs: Triton's bundled libdevice changes 131,367 erf results and 85,005 GELU results, regardless of the tested compiler fusion flag. Native composed PyTorch GELU matches `F.gelu` exactly on the same inputs.

The libdevice shipped in NVIDIA's [CUDA NVCC 12.8.93 package](https://pypi.org/project/nvidia-cuda-nvcc-cu12/12.8.93/) matches native erf and GELU on every tested input under both flags. The official wheel was downloaded and inspected independently before installation; its source, wheel hash and extracted-library hash are saved in `results/gelu_libdevice_source.json`. The accepted helper requires package version 12.8.93 and library SHA-256 `25498d6e3c64782833ff15a5f56665ff09e5d21177163148a1cf008c61733449`. Only the fused reduction receives this `extern_libs` path. Triton's global bundled library, PyTorch, FP32 weights and model architecture remain unchanged. This establishes measured agreement in the pinned environment, not a guarantee for arbitrary library revisions.

The supported `ffn_backend="triton"` option uses two-stage ordered matrices for the two 24-row FFN shapes. It fuses the decoder's final ordered reductions with GELU and with scaled residual addition, preserving separate multiplication/addition rounding. Encoder FFNs use two-stage matrices and their existing activation/residual operations. Initial all-fused model reports remain as `*_all_fused.json`: an initial amortized comparison showed a small encoder regression, while repeated timings overlap. The selected option therefore retains the encoder's existing epilogues. Local/global norm, linear and LayerScale hooks use the original FFN module path, including the LayerScale call that residual fusion normally bypasses. Custom activations/norm forwards and unsupported shapes/layouts use existing fallbacks. Context restoration includes partially installed attributes and graph lifetime remains tied to the matrix runtime.

`results/ffn_components.json` passes **192 eager/graph comparisons**, with every eager output also bitwise equal. It covers both learned matrix weights, twelve activation patterns and four pipeline/fusion variants. Previous versus fused-two-stage warm component medians are 0.02101 → 0.01918 ms for expansion/GELU and 0.02173 → 0.01906 ms for contraction/residual. Under cache eviction these become 0.04506 → 0.04301 ms and 0.04915 → 0.04506 ms. Component gains are not multiplied into historical codec speedups.

`results/full_ffn_runtime.json` passes **27 full-checkpoint cases** against original eager codes, encoder hidden states and decoded audio, including graph replay and restored-model checks. Forty interleaved pairs per direction and geometry use one packed-weight lifetime. At batch eight / three frames, encoder medians are essentially unchanged (12.97151 → 12.97163 ms), while decoder medians fall 11.51078 → 11.40390 ms. Batch 24 / one frame gives 13.98575 → 13.77766 ms encode and 12.56050 → 12.47687 ms decode; batch one / 24 frames gives 12.97921 → 12.79452 ms and 11.33423 → 11.21120 ms. These distributions overlap and single-call independent medians can exaggerate small differences.

`results/full_ffn_ablation.json` separates previous, stage-only, full-fusion three-stage, full-fusion two-stage, and selected dispatch. Eight rotating-order rounds each time twenty owned graph calls after a twenty-call warmup. Previous → selected medians at batch eight / three frames are **13.18486 → 13.13726 ms encode (1.0036×)** and **11.70904 → 11.61315 ms decode (1.0083×)**. Five graphs are resident per direction in this experiment. It times input copies and output ownership but excludes loading, packing, capture and restoration. The equivalent decoder variants also vary between samples, reinforcing the limited precision of these small gains.

`results/full_fidelity_ffn_cute.json` passes all 15 cases with CuTe residuals. `results/full_incremental_ffn.json` retains exact **11,072 tokens and 664,320 samples** across ten uneven requests, with pauses, partial tails, late completion, lane reuse and two requests beyond ten seconds. One/three-fragment schedules take **853.404/856.851 ms encode** and **795.444/796.941 ms decode**. These are logical streaming fidelity schedules, not a new backend ablation or network latency measurement.

The final profile records **1795 encoder kernels and 1059 decoder kernels**, removing 64 decoder launches. Each direction still has 64 ordered main loops; encoder has 64 ordinary reductions and decoder 64 fused reductions. Vendor SGEMM, ordered work and vendor split reductions together occupy approximately **80.74% encoder / 84.77% decoder** kernel time. The decoder ordered group includes fused GELU/residual arithmetic, so it is not pure GEMM attribution. Host work and copies are excluded. Dense arithmetic and weight traffic remain the dominant constraint; this pass does not demonstrate the requested 100× whole-model result.


## Expanded ordered attention and FFN matrices

The accepted FFN epilogues leave dense work dominant. This pass targets the repeated `(24,3840,1280)` attention input projection and the 768-channel transformer matrices at 24, 48, 96 and 192 rows. Triton's [matrix multiplication tutorial](https://triton-lang.org/main/getting-started/tutorials/03-matrix-multiplication.html) motivates tile selection and locality-aware work ordering. Its published FP16 results do not establish performance for this FP32 codec. NVIDIA's [floating-point discussion](https://docs.nvidia.com/cuda/floating-point/index.html) explains why the ordering of products, fused additions and partial sums must be checked separately.

`results/ordered_shapes.json` probes **18 shapes, 131 partition choices and 65 tile variants**. Thirteen shapes admit an exact partition among the tested choices. The attention input projection needs **96-term partitions**, rather than the existing FFNs' 256. Other accepted shapes use 96, 128, 160, 192 or 288 terms. The kernel masks incomplete final partitions and adds partial results in order. Five shapes have no exact partition in this bounded search and remain native: `(24,1280,1280)`, `(48,3072,768)`, `(48,768,768)`, `(192,3072,768)` and `(192,2304,768)`. This is evidence about these tested candidates, not proof that an exact replacement cannot be written.

`results/ordered_shapes_confirm.json` repeats the two best initial tiles at pipeline depths two/three, plus the preceding cuBLASLt/native baseline. All **1,560 eager/graph comparisons** are exact; eager outputs also match bitwise. Each actual learned weight is tested on twelve patterns, including zero, subnormal-scale, tiny, large, random, sparse, scaled and negated inputs. Three alternating warm/cache-evicted timing rounds support selection. `ordered_shapes_selection.json` records selection by lowest median evicted latency, then warm latency. The initial search has cold-start baseline outliers and is not the accepted timing source.

The existing `matrix_backend="triton"` now supports **fifteen** shapes including its original two FFNs. `fast_moss.ordered_matrices.CONFIGS` records each geometry, reduction partition, tile, warp count and pipeline depth. Default FFN configurations remain unchanged, including the explicit two-stage override in the prior FFN option. The helper validates the complete matrix shape and uses ceiling division for partial tails. The model/GPU/compiler/library gates, FP32 weights and arithmetic remain in force.

Three accepted geometries have no cuBLASLt profile entry: `(48,2304,768)`, `(96,768,768)` and `(192,768,768)`. The matrix owner discovers their weights through the ordered configuration, packs lazily, and records per-stream warmup without creating a vendor plan. Other geometries still follow the native contiguous-weight fallback. Cleanup and graph invalidation handle absent vendor plans, including body exceptions and capture-before-warmup errors. The runtime keeps one persistent weight copy; more weights can change storage layout, but packing is not an additional full copy retained alongside them.

`results/full_ordered_shapes.json` contains **27 full-checkpoint cases**, all codes/encoder hidden states/waveforms exact in eager execution, graph replay and after restoration. Its first forty-pair timing experiment exposed a packing confound: enabling new shapes first packs weights that the old runtime kept native. Replaying the old graph within this shared lifetime then copies those weights back to contiguous layout on every call. These timings are explicitly labeled diagnostic and are excluded from speedup claims. Exactness evidence remains valid.

The corrected `results/full_ordered_shapes_ablation.json` gives each backend an independent optimized context and restores storage between them. Three rounds alternate backend order; each direction uses five samples of twenty owned graph calls after forty warmup calls. Only one graph is live at a time. Original eager references and restored outputs are checked every round. Timings include input copies and output ownership, excluding model load, packing, capture and restoration. At batch eight / three frames, **13.167 → 11.520 ms encode (1.143×)** and **11.601 → 9.958 ms decode (1.165×)**. Batch 24 / one frame gives **1.134× / 1.144×**, batch one / 24 frames **1.142× / 1.167×**, while batch one / three frames improves only **1.009× / 1.009×**. Peak allocated memory is **7.715 GB**. At batch eight, 283,115,520 more bytes of existing weights are packed, explaining the shared-lifetime confound; these bytes are not an additional persistent weight copy.

`results/full_fidelity_ordered_shapes_cute.json` passes all **15 cases** with CuTe residuals. `results/full_incremental_ordered_shapes.json` retains exact **11,072 tokens and 664,320 samples** across the ten-request incremental schedule, including pauses, tails, late completion, stable lane reuse and two requests beyond ten seconds. One/three-fragment timings are **780.641/781.489 ms encode** and **716.242/718.831 ms decode**. These stream schedules are fidelity evidence and are not independently paired against the previous backend or a network latency claim.

`results/ordered_shapes_resources.json` verifies all fifteen runtime configurations against actual inputs and inspects their compiled PTX. Main loops use **65–254 registers**, no spills, and no matrix Tensor Core instructions. `results/full_ordered_shapes_profile.json` records **192 ordered main loops per direction**, encoder 192 ordinary reductions, decoder 128 ordinary plus 64 fused reductions. Total kernels rise **1795 → 1863 encode** and **1059 → 1127 decode** because some native operations need an additional reduction kernel. Vendor SGEMM plus ordered work plus vendor split reduction still accounts for **78.58% / 81.88%** of device kernel time. The decoder group includes fused GELU/residual work and excludes host operations/copies; reduced SGEMM naming alone is not counted as reduced dense work. Remaining native reduction orders and the now larger ordered-matrix share are concrete next targets. This pass does not establish 100× whole-model acceleration.


## Completing the five native transformer shapes and refreshing the total comparison

The preceding expansion left five shapes native because its bounded partition search found no exact candidate. All five lacked cuBLASLt profile entries, so the search tried only powers of two and the full reduction size; it could not derive intermediate partition sizes from saved vendor algorithms. This is a search-coverage limitation, not evidence that their arithmetic requires an entirely different kernel.

`benchmarks.ordered_remaining` profiles native launches and tests every multiple of 32 through K. CUTLASS's [efficient GEMM documentation](https://github.com/NVIDIA/cutlass/blob/main/media/docs/cpp/efficient_gemm.md#parallelized-reductions) describes splitting K to expose parallel work and the tradeoff of a separate reduction. Its [SIMT thread multiply source](https://raw.githubusercontent.com/NVIDIA/cutlass/main/include/cutlass/gemm/thread/mma_sm50.h) also makes the accumulation loop explicit. These references motivate the experiment; they do not prove the proprietary cuBLAS build uses identical source or arithmetic.

The first one-call profiler traces omitted some events. The final report profiles ten native calls per shape and records complete observed event counts: ten main loops plus ten reductions for each of the first three shapes, and ten main loops without separate reductions for each 192-row shape. Native main-loop grid-Z values are **14, 8, 8, 4, 5** for `(24,1280,1280)`, `(48,3072,768)`, `(48,768,768)`, `(192,3072,768)` and `(192,2304,768)`, respectively. Rounding `ceil(K/gridZ)` up to 32 suggests **96, 96, 96, 192, 160** terms. The launch-based inference is confirmed empirically: these are the exact partitions in the **136-candidate** search. `results/ordered_remaining.json` retains the launch metadata, all arithmetic differences and 25 tile trials.

`results/ordered_remaining_confirm.json` compares the two best initial tiles at two pipeline depths with native FP32 linear operations. All **600 eager/graph comparisons** pass; eager bit patterns match as well. The twelve activation variants include actual, negated, scaled, zero, subnormal-scale, tiny, large, sparse and random inputs. Three alternating warm/evicted rounds confirm improvements. Selected warm component medians fall **19.27 → 7.17 µs**, **29.43 → 15.42 µs**, **9.78 → 5.83 µs**, **70.82 → 41.45 µs** and **51.95 → 34.47 µs**, respectively. Evicted medians fall **26.62 → 16.38 µs**, **36.86 → 24.58 µs**, **16.38 → 12.29 µs**, **75.78 → 49.15 µs** and **55.30 → 40.96 µs**. These components exclude full-codec work. Selection and prior configuration are saved separately.

The supported ordered configuration grows from **15 to 20 shapes**, reusing the existing main loop, ordered reduction, managed packing and absent-vendor-plan support. No arithmetic code, precision, weight content, optional dependency or model architecture changes. Tests automatically exercise all five added shapes through random-weight rounding, partial tails, module hooks, fallback layouts, storage restoration and graph lifetimes. Environment and checkpoint gates remain unchanged.

`results/full_ordered_remaining.json` passes all **27 full-model cases** against original eager outputs, graphs and restored execution. Its fidelity-only mode avoids the known shared-storage performance confound. `results/full_ordered_remaining_ablation.json` uses the recorded 15-shape baseline from commit `827e69b`, validates that prior configurations have not changed, and creates independent packing lifetimes. Three alternating rounds each contain five samples of twenty calls, with one live graph at a time. At batch eight / three frames, encoder is **11.530 → 10.448 ms (1.104×)** and decoder **9.943 → 8.895 ms (1.118×)**. Batch 24 / one frame gives **1.095× / 1.110×**, batch one / 24 frames **1.106× / 1.129×**, while batch one / three frames is effectively unchanged (ratios **0.9985× / 0.9995×**). The latter does not exercise these five shapes. Peak allocation is **7.729 GB**. Another **1,098,907,648 bytes** of existing weights use packed layout at batch eight, without retaining a second persistent weight copy.

A fresh whole-codec comparison avoids multiplying these increments into historical upstream numbers. `benchmarks.codec_compare` times original eager, original CUDA graph and current optimized graph execution on identical speech windows at batches one/eight, three frames each. Three rotating-order rounds use ten single-call samples per direction/mode after three warmups, independent restored contexts and one live graph at a time. Graph measurements include input copies and owned outputs, excluding load, packing, capture and restoration. Every code, hidden-state, waveform and restored-output comparison is exact.

`results/full_codec_current.json` records batch-one encoder **47.600 / 11.801 / 8.513 ms** and decoder **37.919 / 10.159 / 7.093 ms** for original eager / original graph / optimized graph. Total speedups versus eager are **5.59× / 5.35×**; against the original graph they are **1.39× / 1.43×**. At batch eight, encoder is **49.684 / 18.852 / 10.287 ms**, decoder **39.498 / 17.182 / 8.732 ms**. Corresponding speedups are **4.83× / 4.52×** versus eager and **1.83× / 1.97×** versus the original graph. Peak allocation in this experiment is **7.638 GB**. These measured totals supersede estimates from multiplying component or incremental gains and remain far below 100×.

The latest profile (`full_ordered_remaining_profile.json`) has **272 ordered main loops per direction**, encoder 272 ordinary reductions and decoder 208 ordinary plus 64 fused reductions. Total kernels rise by 24 to **1887 encode / 1151 decode**, reflecting separate reductions for two formerly single-kernel native shapes. Vendor SGEMM alone falls to **8.63% / 2.18%**, while ordered matrix-associated work reaches **67.27% / 77.00%**. Together with vendor split reductions, matrix-associated groups remain **76.30% / 79.20%** of device kernel time. The decoder ordered group includes its GELU/residual epilogues. This locates the next bottleneck in the ordered main loops and their memory traffic, rather than interpreting renamed SGEMM work as eliminated computation. Small-row geometries used by batch-one short chunks and further exact matrix fusion remain open targets.

`full_fidelity_ordered_remaining_cute.json` passes the **15-case CuTe corpus**. The incremental stream report retains exact **11,072 tokens and 664,320 samples** through pauses, partial tails, late completion, lane reuse and two requests longer than ten seconds. One/three-fragment logical schedules take **728.916/729.502 ms encode** and **666.153/667.735 ms decode**; these are not a separate paired streaming speedup or network claim. Compiled resources for all twenty configurations confirm **65–254 registers**, zero spills and no matrix Tensor Core instructions. The complete suite passes **259 tests in 36.56 seconds**, and the wheel matches all 22 runtime/profile files byte-for-byte.

## Small-row tiled cyclic arithmetic

The native short-chunk launches differ from the split-K SGEMMs used by the twenty accepted ordered shapes. Ten-call profiler samples in `results/small_matrix_orders.json` show GEMV for one row, and `gemmSN_TN_kernel` for 3/6/12 rows. The latter uses 128-thread blocks, eight output columns per grid-X block, and one/two grid-Y blocks for 6/12 rows. CUTLASS's [SIMT core](https://github.com/NVIDIA/cutlass/blob/main/include/cutlass/gemm/threadblock/default_mma_core_simt.h) and [GEMV kernel](https://github.com/NVIDIA/cutlass/blob/main/include/cutlass/gemm/kernel/gemv.h) illustrate thread-level accumulation and reduction choices; they are not the cuBLAS implementation.

The initial search (`small_arithmetic.json`, `small_arithmetic_wide.json`) tested 21 and 336 configurations on the first 128 columns of the original full `(3,5120,1280)` operation. Lane counts, contiguous vector lengths, separate/single accumulators and serial/butterfly reductions all failed exact equality. Local inspection with the installed NVIDIA `cuobjdump` of the SM120 small-N kernel then motivated a different hypothesis: **reset each lane's FMA accumulator every 256 K elements, add successive tile results within each lane, then add the 16 lanes serially**. The vendor assembly is not included in this repository; the probes implement the inferred arithmetic independently.

For output `(m,n)`, lane `l=0..15` computes, in increasing `s`, `a[l,t] = fma(x[m,256*t+16*s+l], w[n,256*t+16*s+l], a[l,t])`, starting each tile at zero. It then computes `b[l] = (((0 + a[l,0]) + a[l,1]) + ...)` in increasing tile order. Finally, the output is `(((b[0]+b[1])+b[2])+...)`. Reordering the lane and tile sums changes the result. Masked tail terms contribute zero. `small_tiled_arithmetic.json` checks all captured small-row shapes and full output widths at tile sizes 128/256/512: exactly the 256-element hypothesis matches all **18** 3/6/12-row shapes, including K=384/640 tails. All six one-row cases still fail, so this arithmetic does not cover GEMV.

The initial exact implementation repeats weights per input row and uses an expensive lane extraction. `small_tiled_tune.json` records 16 tile/warp configurations per selected shape; every simple candidate remains slower than native. Subsequent research probes share weights across rows and gather each lane directly (`small_tiled_grouped.py`), or compute each 256-element tile independently and perform the same ordered tile/lane reduction in a second kernel (`small_tiled_split.py`). Warm component timing alone does not establish a model gain or justify enabling a candidate. The selected implementations subsequently passed the gates below before runtime integration.

`small_tiled_confirm.json` contains 480 eager/graph comparisons (including native controls), with twelve actual/random/sparse/extreme activation variants and bit-pattern checks. Three alternating warm/evicted rounds retain three shapes: `(3,5120,1280)` uses grouped rows `(4,8,4,4)`; `(3,1280,5120)` and `(12,768,3072)` use split tiles `(4,8,4)`. Their warm ratios are approximately 1.18×/1.53×/1.10×; evicted ratios are 1.05×/1.15×/1.11×. The tested six-row FFN candidates remain slower and are excluded.

`fast_moss/small_matrices.py` packages only those three shapes. The managed Triton matrix runtime uses them while parameter storage is contiguous, requires stream warmup before capture, and retains no parameter copy or persistent scratch. A later packed-matrix call invalidates prior managed graphs, and subsequent small calls follow the existing contiguous-weight fallback. Context entry/exit, hooks, unsupported layouts, environment validation and restoration keep the existing lifetime rules. `test_small_matrices.py` checks random weights, seven numerical scales, native storage identity, hooks, singleton-stride fallback, graph warmup, later packing and invalidation.

The research wrapper and then the actual runtime each pass 27 full-checkpoint cases. The final integrated independent-context comparison in `full_small_runtime.json` uses three alternating rounds, five samples of ten graph calls per direction, and the previous backend from `ba4efdd`. Batch one / three frames improves **8.631 → 8.517 ms encode (1.0134×)** and **7.188 → 7.059 ms decode (1.0183×)**. Batch two / three frames improves about 0.5% each; batch one / six frames about 0.7%/0.3%. The one-frame control is unchanged within measurement variation. Input copies and owned outputs are timed; loading, capture and context setup are excluded. Peak allocation is 7.714 GB. These small incremental gains do not establish 100× whole-model acceleration.

The integrated kernels also pass 27 CuTe cases and a 12.96-second single-lane stream with three-frame chunks, including the ten-second cache boundary. All 5,184 tokens and 311,040 samples equal corrected eager streaming. The profiler now separates small-row kernels and vendor small-N/GEMV work without counting `cublasGemmSmallNParams` again as SGEMM. At batch one / three frames, remaining vendor small matrices consume 29.95%/34.02% of encode/decode kernel time; the new kernels consume 31.01%/38.02%. Matrix work remains dominant. Compiled kernels use 40–64 registers, zero spills and no matrix Tensor Core instructions. Fresh total gains versus original eager are 5.71×/5.48× at batch one, with all original-reference checks exact; the 100× objective is still unmet.

## Explicit row accumulators and CUDA vector loads

The next experiment keeps the inferred 256-element/tile/lane arithmetic but changes how weight vectors are shared across input rows. NVIDIA's [vectorized-memory-access guidance](https://developer.nvidia.com/blog/cuda-pro-tip-increase-performance-with-vectorized-memory-access/) explains the instruction-count benefit and register/alignment tradeoffs of wider loads. Its [matrix-multiplication background](https://docs.nvidia.com/deeplearning/performance/dl-performance-matrix-multiplication/index.html) motivates treating small-row memory traffic as a primary cost. These sources motivate hypotheses; they do not guarantee a speedup for this model.

`small_fixed_rows.py` assigns a separate accumulator to each row, avoiding a mandatory power-of-two row tensor axis. It tests three/six-row tiles and padded controls, different column tiles, warp counts and loop unrolling. All **198** configurations across four representative matrices are exact on captured inputs. Best warm component times fall from 10.19 to 8.35 µs for `(6,3072,768)`, 15.33 to 10.65 µs for `(6,768,3072)`, 10.81 to 8.76 µs for `(3,3840,1280)`, and 14.04 to 10.49 µs for `(12,2304,768)`.

`small_vector_cuda.py` independently implements the same arithmetic in CUDA C++, compiled by installed **NVRTC 12.8**. A thread holds one/two/four consecutive accumulation lanes, allowing scalar/float2/float4 weight loads without interchanging FMA or final reduction order. It tests direct versus shared activation loads, block sizes and unrolling; all **144** trials are exact. The best vectorized six-row expansion is competitive in warm timing, but the long-K projection regresses. The selected Triton implementations require no NVRTC or CUDA-binding runtime dependency. CUDA probes remain research-only. CPU-only SASS inspection of the vectorized expansion confirms **16 `LDG.E.128.CONSTANT`**, **96 `LDS.128`** and **384 FP32 `FFMA`** instructions in the compiled body (`small_vector_instructions.json`); wider memory instructions alone do not establish a runtime gain.

`small_memory_confirm.json` checks **480 eager/graph comparisons** with actual/random/sparse/extreme activations and bit-pattern comparisons. The warm winners are not uniformly good with evicted weights. `small_memory_cold.json` retains a 342-configuration cold-only diagnostic with graph warmup but no sustained compute warmup. Its absolute timings are not mixed with the warm-before-eviction confirmation protocol. The tool now exposes this difference explicitly through `--warm-ms`; the recorded diagnostic used zero sustained warmup. No measured clock-state cause is asserted.

The first four-shape full-model probe (`full_small_memory.json`) passes 27 cases. Its three rotating backend orders compare all four candidates, only the long-K candidate, and the recorded runtime from `3cec6b1`. All four improve batch-one / three-frame encode/decode by approximately 1.6%/1.8%; the long-K-only set gives 0.9%/1.4%. This supports a wider search rather than deciding from isolated cache timing alone.

`small_fixed_shapes.json` covers all **18** captured 3/6/12-row shapes with **96** configurations; all outputs are exact. `small_fixed_confirm.json` then records **960 eager/graph comparisons**, twelve activation variants, compiled resources and three matched warm/evicted rounds for sixteen promising shapes. The broader model probe compares sixteen warm-selected replacements with an eleven-shape subset whose confirmed evicted results are within 5% of the preceding implementation. Both preserve exact outputs; the eleven-shape set performs more consistently across the tested model geometries. `small_fixed_selection.json` records that choice. Two existing configurations change, nine new shapes are added, and one old split kernel is retained, for **twelve** native-layout small shapes alongside the unchanged twenty ordered packed shapes.

The integrated runtime (`full_small_fixed_runtime.json`) passes 27 original-reference cases and improves batch-one / three-frame encode **8.505 → 8.239 ms (1.0323×)** and decode **7.063 → 6.794 ms (1.0395×)**. Batch two / three frames gives 1.0038×/1.0037×; batch one / six frames 1.0054×/1.0047×; the one-frame control is unchanged. Three alternating independent contexts use five samples of ten owned graph calls per direction, including copies/outputs and excluding loading/capture/setup. Peak allocation is 7.714 GB. Baseline snapshots and the `previous_runtime` research context keep old controls independent of later configuration changes. These are incremental improvements, not a 100× claim.

Final validation retains exact 27-case CuTe output, a 12.96-second batch-one stream (5,184 tokens / 311,040 samples), and the incremental schedule (11,072 tokens / 664,320 samples). The full suite passes **285 tests**. All twelve compiled small configurations use **40–128 registers**, zero spills and no matrix Tensor Core instructions. The batch-one profile has 144 explicit-row kernels and 32 retained split/reduction pairs per direction, reducing total kernel counts by twelve to 1666 encode / 1027 decode. New small kernels account for 51.65%/61.95% of device kernel time; remaining vendor small matrices account for 7.13%/8.94%. Moving work between named groups is not itself a speedup. Fresh original-eager comparisons give 5.76× encode / 5.56× decode at batch one; original-graph comparisons give 1.47×/1.52×. The 100× objective remains unmet.

## Exact one-row cyclic GEMV

The next target is the shortest batch-one chunk, which the twelve accepted 3/6/12-row shapes do not improve. The [KBLAS paper](https://arxiv.org/abs/1410.1726) discusses GPU BLAS-2 thread organization and double buffering; it motivates inspecting memory traffic and reductions, but does not establish the arithmetic order of this pinned cuBLAS implementation. `small_matrix_orders --rows 1 --all-captured` records the actual six native launches in `gemv_native_orders.json`. Their block dimensions are 32×16 for `(1,768,1280)`, 16×32 for the three `(1,1280,K)` shapes, and 8×4 for `(1,3840,1280)` / `(1,5120,1280)`.

The 336 first-128-column hypotheses in `gemv_arithmetic.json` identify eight cyclic lanes with a halving reduction for the expansion shape. CPU inspection of the installed SM120 GEMV variants with NVIDIA `cuobjdump` motivates a full-width search. Vendor assembly is not redistributed. Variant 9 reports 128 registers, a 32-byte stack and 1,024 shared bytes; its static listing includes local-memory instructions. These observations are not a measured explanation of latency. `gemv_order.json` tests 48 full-width hypotheses and identifies **32 lanes** for `(1,768,1280)`, **16 lanes** for the three `(1,1280,K)` shapes and **8 lanes** for the two expansions.

For each output column, lane `l` accumulates `a[l] = fma(x[s*L+l], w[n,s*L+l], a[l])` in increasing `s`, starting at +0 and spanning the whole K dimension. The explicit reduction combines lane pairs at distances `L/2`, `L/4`, …, `1`; the output is lane zero. This differs from the 256-term accumulator resets and serial lane sum used by the multi-row kernel. A generic Triton `sum` fails exactness for the 32-lane shape in the tested layout, so runtime kernels use the explicit tree for every shape.

A final floating-point addition of **+0** is also required. With negative minimum-subnormal activations and positive 0.125 weights, omitting that operation can produce −0 while the native result is +0. Ordinary numerical equality hides this discrepancy. `gemv_zero.json` contains sixty comparisons, with and without the final addition: all thirty bit-pattern checks with it pass. The runtime uses explicit `add.rn.f32` because an ordinary source-level addition of zero can be eliminated. Precision remains FP32, with explicit FMA and no Tensor Core approximation.

`gemv_tune.json` records **195 exact launch configurations**, varying output columns, warps and loop unrolling. `gemv_confirm.json` tests three finalists plus native control on fourteen activation variants, totaling **672 eager/graph comparisons**, all bit-exact. It includes actual, negated, scaled, sparse, zero, negative zero, subnormal, large and random inputs. Three alternating rounds measure both warm and 128 MiB-evicted executions. For K=5120 the first warm native measurement was 68.8 µs; repeated confirmation gives 35.7 µs, so the initial apparent 6.7× result is not retained. Selected warm gains are about **1.69–3.51×**; evicted gains are only **1.00–2.09×**. These component ratios are not whole-model gains.

`exact_gemv_model.py` snapshots the previous twelve small and twenty ordered configurations from `f846998` in `gemv_baseline.json`. Its research wrapper and integrated runtime modes restore independent contexts between candidates and baseline, keeping the older `gemv_model.py` arithmetic experiment intact. Selection requires a warm improvement and no more than 5% evicted regression, then prefers the best evicted median. All six shapes qualify. The research wrapper passes 27 full-model cases and measures batch-one / one-frame **7.527 → 7.445 ms encode (1.0110×)** and **6.687 → 6.600 ms decode (1.0133×)**. Controls at batch two / one frame, batch one / three frames and batch eight / one frame do not exercise the new shapes.

`small_matrices.py` adds the six native-layout GEMV configurations, bringing the table to eighteen shapes. They use the same managed dispatch, per-stream warmup, capture invalidation, hooks and restoration rules, without packing or persistent scratch. One-dimensional inputs are newly eligible; tests compare vectors, matrices and rank-three inputs against original `Linear`, including graph replay and forced signed-zero underflow. Together with the table-driven random-weight and storage tests, **44 focused tests pass**. The wheel matches all 23 runtime/profile files byte-for-byte.

The actual integrated-runtime comparison (`full_gemv_runtime.json`) passes all **27 cases** and measures one-frame encode **7.520 → 7.417 ms (1.0139×)** and decode **6.686 → 6.598 ms (1.0134×)**. Controls vary by approximately −0.5% to +0.3%, with no new-shape dispatch. The experiment uses three alternating independent contexts and five samples of ten graph calls per direction, including copies and owned outputs while excluding setup, capture and restoration. These results establish a small incremental gain, not the requested 100× whole-model result.

The integrated CuTe path passes **27 cases** (`full_gemv_cute.json`). A **12.96-second stream in 162 one-frame chunks** exercises GEMV across the ten-second cache boundary: all **5,184 tokens and 311,040 samples** are exact against corrected original eager streaming. Encoder codes also match offline; both original and optimized streaming decoding differ from offline by at most **1.349e-6**, identically. `full_incremental_gemv.json` retains exact **11,072 tokens and 664,320 samples** through pauses, tails, lane reuse and long requests. One/three-fragment schedules take **729.165/731.337 ms encode** and **666.378/668.599 ms decode**; this is a regression gate, not a new paired streaming or network-latency claim.

The one-frame profile (`full_gemv_profile.json`) contains **129 GEMV kernels for encoding and 128 for decoding**, among **1548 / 944 total kernels**. No packed ordered or multi-row small kernels run at this geometry. GEMV consumes **44.27% / 50.61%** of kernel time, remaining vendor small matrices **26.43% / 28.82%**, and all disjoint matrix groups **71.56% / 80.44%**. This identifies the remaining vendor small matrices and GEMV weight traffic as subsequent targets. Relabeling vendor work as Triton work does not imply eliminating its cost. All eighteen configurations pass actual-weight bit checks; the six new GEMVs use **39–80 registers**, **16–640 shared bytes**, zero spills and no matrix Tensor Core instructions (`gemv_resources.json`).

The fresh **one-frame** upstream comparison in `full_codec_single_frame.json` measures batch-one encoder **47.008 / 10.344 / 7.346 ms** and decoder **37.312 / 9.107 / 6.523 ms** for original eager / original graph / current optimized graph. Total ratios are **6.40× / 5.72×** versus eager and **1.41× / 1.40×** versus original graphs. Batch-eight encoder is **46.869 / 13.899 / 10.802 ms**, decoder **37.990 / 12.492 / 9.595 ms**, or **4.34× / 3.96×** versus eager and **1.29× / 1.30×** versus original graphs. All outputs and restored executions are exact, with peak allocation **7.517 GB**. The three-frame headline remains the preceding direct measurement of that unchanged dispatch geometry; these results do not multiply historical incremental ratios.

The complete suite passes **303 tests in 48.02 seconds**. This includes the six new vector/signed-zero cases and twelve added table-driven cases, beyond the preceding 285 tests. The requested 100× whole-model acceleration remains unachieved.

## Two-, four- and eight-row matrix search

The one-frame profile after `e5c974a` leaves 26–29% of kernel time in vendor small matrices. The saved checkpoint operands already include **26** shapes with two, four or eight rows, so no replacement or recapture of the existing input artifact is needed. The native launches (`short_native_orders.json`) all use 128-thread blocks and eight output columns per grid-X block. `short_tiled_arithmetic.json` checks 128/256/512-term tiles across every full output: the **256-term, sixteen-lane, serial-lane-sum hypothesis matches all 26 shapes bit-for-bit**, including the K=240/384/640 tails.

NVIDIA's [CUTLASS execution guide](https://docs.nvidia.com/cutlass/latest/media/docs/cpp/efficient_gemm.html) explains the tradeoff between larger tiles for data reuse and smaller tiles for usable parallelism, and describes partitioning K when the output grid is too small. Its [matrix multiplication guide](https://docs.nvidia.com/deeplearning/performance/dl-performance-matrix-multiplication/index.html) relates matrix geometry to arithmetic intensity. These motivate testing row sharing and split reductions; they do not establish exactness or speed on this checkpoint. `short_rows.py` independently compares explicit two/three/four/eight-row accumulators, output-column tiles, warp counts and loop unrolling, plus split 256-term tiles where appropriate. Every candidate preserves the measured native FMA, tile-sum and lane-sum order; generic parallel sums would not satisfy that requirement.

`short_rows.json` contains **558 launch configurations**, all bit-exact and without spills. `short_confirm.json` checks finalists from 25 shapes on fourteen activation variants, with **2,380 eager/graph comparisons** including native controls; every bit check passes. Three alternating warm/evicted rounds reject the large first-pass cold outliers: for example `(8,768,768)` native drops from an initial 222.4 µs to a repeated median of 16.384 µs. That initial denominator is not a retained speedup. Twenty configurations satisfy the warm-improvement and ≤5% evicted-regression gates; their confirmed warm ratios span **1.077–1.704×**, and evicted ratios **0.964–1.545×**.

`short_model.py` expands the model corpus to **39 cases**, adding two-, four- and eight-lane one-frame inputs and batch-one two-frame inputs for music, speech and environmental audio. It snapshots the preceding eighteen small and twenty packed ordered configurations in `short_baseline.json`, and uses independently restored contexts. All 39 cases pass the full original-reference codes, hidden-state, waveform, graph and restoration checks. Three rotating orders compare previous runtime, the 25-shape warm selection and the 20-shape conservative selection. The latter is faster at all affected measured geometries: batch-one one-frame **7.422 → 7.141 ms encode (1.0394×)** and **6.629 → 6.315 ms decode (1.0497×)**; batch two **1.0291× / 1.0302×**; batch eight **1.0460× / 1.0540×**. The three-frame control does not exercise the new shapes. The twenty-shape selection wins despite having fewer warm-component winners, showing why full-model/cache behavior is a required gate.

`short_selection.json` records the accepted twenty additions: **sixteen explicit-row and four split-tile configurations**. The runtime table now has **38 native-layout shapes**, retaining all eighteen previous configurations and all twenty packed ordered configurations. No arithmetic kernel, checkpoint, precision, persistent weight ownership or dependency changes are required. Existing table-driven tests now cover the new dimensions, inner-dimension tails, native storage, hooks, singleton-stride fallback and graph lifetime rules.

The actual supported-runtime gate (`full_short_runtime.json`) also passes all **39 cases**. Its matched one-frame encode/decode medians are **7.421 → 7.146 ms / 6.603 → 6.322 ms** at batch one (**1.0385× / 1.0445×**), **8.408 → 8.208 ms / 7.175 → 6.970 ms** at batch two (**1.0243× / 1.0294×**), and **10.962 → 10.449 ms / 9.739 → 9.242 ms** at batch eight (**1.0491× / 1.0537×**). The unaffected three-frame control is **1.0021× / 0.9990×**. Each direction uses three alternating independent contexts and five samples of ten graph calls, including copies and owned outputs; peak allocation is **7.712 GB**. The 84 focused tests pass, and the wheel matches all 23 runtime/profile files byte-for-byte.

`full_short_cute.json` passes the same **39-case** corpus. Long-stream validation uses **162 one-frame chunks over 12.96 seconds**, both with one lane and eight simultaneous lanes. All **5,184 / 41,472 tokens** and **311,040 / 2,488,320 audio samples** match corrected original eager streaming, chunk by chunk, across the ten-second cache boundary. Encoder codes also match offline. Original and optimized streaming share the same waveform differences from offline decoding: maximum **1.349e-6** at batch one and **2.703e-5** at batch eight. The eight-lane case peaks at **8.791 GB** allocation. These checks establish exactness against the original streaming behavior, without treating streaming/offline numerical differences as optimization error.

The fresh one-frame original/current comparison (`full_codec_short_rows.json`) measures batch-one encoder **46.539 / 10.348 / 7.070 ms** and decoder **36.810 / 9.097 / 6.247 ms** for original eager / original graph / current graph. Total speedups are **6.58× / 5.89×** versus eager and **1.46× / 1.46×** versus original graphs. At batch eight the corresponding encoder times are **46.210 / 14.003 / 10.296 ms**, decoder **37.346 / 12.508 / 9.088 ms**: **4.49× / 4.11×** versus eager and **1.36× / 1.38×** versus original graphs. All original-reference and restored-output checks pass. Peak allocation is **7.517 GB**. These totals use freshly measured denominators rather than multiplying earlier incremental gains; the three-frame dispatch is unchanged.

The one-frame profile now includes a separate per-kernel breakdown within the small-matrix group, explicitly marked as already included in that group to prevent double counting. Batch one runs **129/128 GEMVs, 61 fixed-row kernels and 36 split/reduction pairs**, among **1584 / 980 total kernels** for encode/decode. The 36 additional launches come from split reductions, despite lower measured codec latency. GEMV still accounts for **46.47% / 52.01%** of kernel time. Remaining vendor small matrices fall to **8.28% / 8.12%**; all disjoint matrix groups together consume **70.74% / 79.48%**. At batch eight there are **128 fixed-row kernels per direction**, plus one encoder split pair, and **1581 / 1038 total kernels**. Vendor SGEMM takes **28.49% / 24.98%**, vendor small matrices **10.18% / 10.70%**, and all matrix groups **78.58% / 80.56%**. This points to still-native 16/32/64-row work in one-frame batches, alongside GEMV memory traffic, as subsequent research targets. Profile names moving from vendor to Triton categories are not themselves evidence of speedup.

`short_resources.json` verifies all **38** runtime configurations against actual checkpoint operands: **36–128 registers**, zero spills and no matrix Tensor Core instructions. The incremental request schedule remains exact for **11,072 tokens and 664,320 samples**, including pauses, tails, lane reuse and long requests (`full_incremental_short.json`). One/three-fragment schedules measure **727.365/729.620 ms encode** and **668.148/668.943 ms decode**; these are regression measurements, not a new paired streaming speedup or network-serving claim.

The full suite passes **343 tests in 56.27 seconds**. The new table adds forty parameterized shape/behavior cases to the preceding 303-test suite. The requested 100× whole-model result remains unachieved.

## Medium-row partitions and signed-zero fidelity

After `31b95eb`, one-frame batch-eight profiles leave roughly a quarter of kernel time in vendor SGEMM. `mid_native_orders.json` covers all twenty captured 16/32/64-row shapes. Six 16-row operations use the small-N family; most 32/64-row operations use CUTLASS SIMT plus split reduction, and `(32,3072,768)` uses a distinct large-K kernel. Full-width arithmetic probes match all twenty shapes: 16 rows use the established 256-term tiled arithmetic; the others use shape-specific sequential-FMA partitions. The large-K exception matches **512 + 256 terms**, rather than equal halves inferred from its two-part launch. `(64,768,240)` requires an **80-term** boundary; rounding that hint up to 96 fails exactness. Launch geometry alone is not sufficient evidence of the arithmetic.

The September 2026 preprint [Taming Bitwise Behavior in GPU Kernels with Tensor Core](https://arxiv.org/html/2609.11356v1) separates reduction boundaries/rounding from scheduling choices and describes reconstructing vendor arithmetic. Its reported matching GEMM family uses fp16, bf16 and fp8 inputs on other GPU configurations, so that result does not validate this FP32 SIMT implementation. NVIDIA's [CUTLASS GEMM API](https://docs.nvidia.com/cutlass/latest/media/docs/cpp/gemm_api.html) describes the separate partition and reduction kernels, while [Triton's dot documentation](https://triton-lang.org/main/python-api/generated/triton.language.dot.html) distinguishes its IEEE and TF32 input modes. This work retains original FP32 operands and IEEE SIMT accumulation.

`mid_rows.json` initially tests **288 configurations**, all exact for captured actual inputs. A stronger underflow audit then exposes a fidelity defect in existing ordered kernels. With negative minimum-subnormal inputs, **42 of 144 eager/graph case pairs** disagree in bit patterns despite numerical equality; **23** failing pairs concern previously supported configurations. The same issue rejects several proposed candidates in `mid_confirm_unfixed.json`. These artifacts explicitly identify their legacy signed-zero policy and are retained as failing evidence.

The native paths differ in their signed-zero behavior. Five existing configurations preserve negative zero; the other measured paths canonicalize it with an addition of +0. Two preserving configurations also have partial tails: padding their loop with extra zero FMAs changes −0 to +0. `ordered_matrices.py` now applies the shape-specific final addition and shortens the affected tail loops. The fused FFN second-projection residual epilogue applies the corresponding addition before scaling; its first-projection GELU path preserves native negative zero. Arithmetic defaults in the low-level research kernels retain the legacy behavior for reproduction, while runtime calls explicitly request the corrected policy.

`mid_zero_fixed.json` passes all **288 eager/graph bit comparisons** across the 72 tested existing/proposed non-GEMV configurations, with actual checkpoint weights and forced positive weights. New parameterized regressions cover ordered matrices and both fused FFN epilogues; the focused matrix/FFN suite passes **89 tests**. The subsequent configuration ablation shares these correctness fixes between both arms: its baseline is the preceding configuration table with corrected arithmetic, not an assertion that the legacy signed-zero behavior is acceptable.

The corrected component confirmation (`mid_confirm.json`) passes **2,072 eager/graph comparisons** across fourteen activation variants and three finalists/control groups. Twenty configurations have warm gains of **1.073–2.683×**; their evicted ratios span **0.650–1.900×**, so this is not a claim that every component is faster with cold weights. A seventeen-shape subset satisfies the stricter evicted gate. `full_mid_rows.json` compares both selections against the preceding tables, with the signed-zero correction shared by all arms and independent packing/restoration lifetimes. All **39 model cases** pass. The full twenty-shape selection is consistently faster in the affected geometries: batch-eight / one-frame **10.463 → 9.228 ms encode (1.1338×)** and **9.239 → 7.990 ms decode (1.1563×)**; batch-four / two-frame **1.1338× / 1.1645×**; batch-one / eight-frame **1.1398× / 1.1682×**. One-frame batch-one and three-frame batch-eight controls remain effectively unchanged. Component eviction regressions therefore do not justify discarding the larger set when the codec measurements favor it.

`mid_selection.json` accepts **six native-layout 16-row configurations and fourteen packed ordered 32/64-row configurations**. The runtime now has **44 native-layout and 34 packed ordered shapes**. It retains the existing single-copy weight ownership, unsupported-layout fallback, stream warmup and managed graph invalidation. The integrated matrix/small-matrix/FFN gate passes **227 tests in 72.08 seconds**. The wheel matches all 23 runtime/profile files byte-for-byte.

The default-table runtime gate (`full_mid_runtime.json`) passes all **39 cases**, and `full_mid_cute.json` passes the same corpus. Actual integrated encode/decode medians are **10.444 → 9.240 ms / 9.241 → 8.021 ms** at batch-eight / one-frame (**1.1304× / 1.1520×**), **10.370 → 9.142 ms / 8.953 → 7.700 ms** at batch-four / two-frame (**1.1343× / 1.1627×**), and **10.273 → 9.030 ms / 8.769 → 7.503 ms** at batch-one / eight-frame (**1.1377× / 1.1687×**). One-frame batch-one and three-frame batch-eight controls vary by less than 0.4%. These configuration increments use three alternating restored contexts, five samples of ten graph calls, copies and owned outputs included; loading, packing and capture are excluded. Peak allocation is **7.740 GB**.

Long-stream gates pass chunk-by-chunk equality against corrected original eager streaming: **41,472 tokens / 2,488,320 samples** over 162 one-frame chunks in eight lanes (`full_mid_streaming_b8.json`), and **5,120 tokens / 307,200 samples** over twenty eight-frame chunks in one lane (`full_mid_streaming_f8.json`). Both cross the ten-second cache boundary. Encoder codes also match offline. Original and optimized streaming share the same maximum waveform differences from offline: **2.703e-5** and **1.125e-6**, respectively. Peak allocations are **8.798 GB / 7.597 GB**. The incremental scheduler retains exact **11,072 tokens / 664,320 samples** through arrivals, pauses, tails and lane reuse. One/three-fragment schedules take **721.725/723.854 ms encode** and **660.962/661.612 ms decode** (`full_incremental_mid.json`); these single-repeat regression measurements are not new paired speedup or network-latency claims.

The batch-eight / one-frame profile (`full_mid_profile.json`) runs **1,581 / 1,038 kernels** for encode/decode, including **98 / 97 ordered main/reduction pairs** and **176 fixed-row kernels** per direction. Vendor SGEMM falls to **8.56% / 1.31%** of kernel time and vendor small matrices to **0.71% / 0%**. Ordered kernels take **14.89% / 16.56%**, native-layout small kernels **52.19% / 59.93%**, and all disjoint matrix groups **76.34% / 77.81%**. The nested small-kernel breakdown is already included in its parent group. Scheduling and weight reuse in our small kernels are now the largest target at this geometry; the remaining encoder vendor SGEMM still needs layer attribution. Kernel relabeling alone is not a performance gain.

Resource audits cover all **44 small and 34 ordered configurations**, with actual-input bit equality, zero spills and no matrix Tensor Core instructions. Small-kernel register counts span **36–128**, ordered main kernels **56–254** (`mid_small_resources.json`, `mid_ordered_resources.json`). The latter records the corrected signed-zero/tail flags.

Fresh original/current comparisons use three rotating rounds of ten single-call samples, including graph input copies and owned outputs, excluding load/packing/capture/restoration. In `full_codec_mid_one.json`, one-frame batch-one encode is **46.758 / 10.343 / 7.064 ms**, decode **36.976 / 9.093 / 6.241 ms**, for original eager / original graph / optimized graph: **6.62× / 5.92×** versus eager, **1.46× / 1.46×** versus original graphs. At batch eight, encode is **46.483 / 13.896 / 9.086 ms**, decode **37.642 / 12.300 / 7.870 ms**: **5.12× / 4.78×** versus eager and **1.53× / 1.56×** versus original graphs.

In `full_codec_mid_three.json`, three-frame batch-one encode is **47.867 / 11.811 / 8.115 ms**, decode **38.253 / 10.169 / 6.707 ms**: **5.90× / 5.70×** versus eager and **1.46× / 1.52×** versus original graphs. Batch-eight encode is **49.835 / 19.070 / 10.164 ms**, decode **39.870 / 17.362 / 8.613 ms**: **4.90× / 4.63×** versus eager and **1.88× / 2.02×** versus original graphs. Every comparison and restored execution is exact. These are direct totals with fresh denominators, not products of historical incremental ratios.

The complete suite passes **419 tests in 64.26 seconds**. Wheel verification matches all **23 runtime/profile files**. This turn is **progress**: twenty measured configurations and a signed-zero correctness repair, with exact corpus and streaming gates. The requested **100× whole-model acceleration remains unmet**.


## Narrow-column grids and explicit row layouts

NVIDIA's [CUDA Best Practices Guide](https://docs.nvidia.com/cuda/cuda-c-best-practices-guide/index.html) describes register allocation, active blocks and independent instructions as interacting constraints: a higher occupancy number alone does not prove faster execution. Triton's [matrix multiplication tutorial](https://triton-lang.org/main/getting-started/tutorials/03-matrix-multiplication.html) demonstrates how tile ordering affects data reuse. These motivate measured scheduling changes; neither establishes exact FP32 arithmetic or performance for this model.

The `fd559c3` profile makes small native-layout matrices the main target at batch eight / one frame. `narrow_rows.py` varies one/two output columns, row tiles and loop unrolling in the existing runtime kernels, retaining every FMA and reduction boundary. The baseline is snapshotted in `narrow_baseline.json`. `small_layout.py` separately explores an explicit Gluon layout: sixteen lanes per column, two columns per warp, and row accumulators held in registers. It supports up to sixteen rows per block. Only lane zero writes after an explicitly ordered sequence of fifteen shuffle/add steps. This is a research probe until stronger fidelity and full-model timing establish whether it belongs in the runtime.

The initial narrow sweep covers **564 configurations across all 44 shapes**, all captured-input bit-exact and spill-free. Confirmation tests 31 promising shapes with fourteen activation variants, including signed zero and underflow: **3,528 eager/graph comparisons pass**. Thirty candidates retain a warm improvement greater than 1%; their warm ratios span **1.013–1.209×**, with evicted ratios **0.740–1.250×**. A 25-shape subset also satisfies the ≤5% evicted-regression criterion.

`full_narrow_rows.json` passes all **48 original-reference model cases**, but rejects either broad set as a universal runtime improvement. The 30-shape selection gives batch-one / one-frame **1.0161× encode / 1.0148× decode**, and batch-two / one-frame **1.0121× / 1.0086×**. It regresses batch-eight / one-frame (**0.9938× / 0.9928×**) and batch-one / eight-frame (**0.9881× / 0.9862×**). The 25-shape selection is generally worse. Three-frame geometries stay near parity. All arms use independent restored contexts, so the regression is not accepted on the strength of warm component timings. The report's original `selection="cold"` field was a runtime-only CLI option; its explicitly named `candidate` and `cold_subset` arms contain the 30-shape warm and 25-shape cold selections. Subsequent reports label research selection directly.

The Gluon probe covers **144 configurations across six eight-/sixteen-row shapes**, all exact on captured operands. The pinned Gluon API requires explicit nested slice layouts and distributed zero tensors for masked loads; those compiler requirements were resolved before execution. Repeated confirmation passes **750 eager/graph comparisons**, including forced-positive weights with negative minimum-subnormal activations. Selected warm ratios are **1.023–1.269×**, evicted ratios **0.875–1.667×**. Initial cold outliers shrink substantially under repeated measurements and are not used as performance conclusions. `full_narrow_groups.json` isolates one-/two-row and short-row subsets and compares a one-/two-row plus explicit-layout combination at model level.

The group study passes all **48 cases** for the one-/two-row plus explicit-layout combination. Isolating the **ten one-/two-row configurations** gives batch-one / one-frame **1.0105× encode / 1.0088× decode**. Its other measured geometries vary by less than 0.3%. The fifteen-shape short-row subset does not establish a useful additional gain, and the sixteen-shape explicit-layout combination regresses batch-one / eight-frame decoding by about 0.6%. Only the ten one-/two-row changes proceed to the default-runtime gate; the Gluon kernel remains research-only. The selection changes four GEMV tiles and six two-row tiles, including replacement of `(2,768,3072)` split computation with one fixed-row kernel. The runtime still supports **44 native-layout / 34 ordered shapes** and now has **four split shapes**.

`weight_storage.py` performs a CPU-only representation audit of six captured one-row checkpoint weights. Only roughly **0.0014–0.0017%** of their elements are exactly representable as BF16; FP16 round-trips also fail for nearly all elements. Casting these weights would therefore violate the exactness requirement. An estimated format that preserves all sign/fraction bits and encodes per-block exponent deltas, including a six-byte header/offset allowance per 256 weights, gives a storage ratio of **1.135–1.138×**. This is only a size estimate: no compression/decompression implementation or GPU speedup has been established (`weight_storage_audit.json`).


The integrated default-table gate (`full_narrow_runtime.json`) passes **48 cases**. Batch-one / one-frame encode improves **7.170 → 7.097 ms (1.0102×)** and decode **6.323 → 6.261 ms (1.0098×)**. Other measured cases stay within 0.3% of parity, including batch-two / one-frame; no additional gain is claimed for them. Three alternating restored contexts time five samples of ten graph calls, including copies and owned outputs, excluding setup/packing/capture/restoration. Peak allocation is **7.747 GB**. Wheel verification matches all **23 runtime/profile files** byte-for-byte.


The CuTe residual combination passes the same **48-case corpus** (`full_narrow_cute.json`). Two streams span **162 one-frame chunks over 12.96 seconds**, with one and two lanes. All **5,184 / 10,368 tokens** and **311,040 / 622,080 samples** match corrected original eager streaming chunk by chunk. Encoder codes also match offline. Original and optimized streaming share the same maximum waveform differences from offline decoding: **1.349e-6 / 1.952e-6**. Peak allocations are **7.526 / 7.696 GB** (`full_narrow_streaming_b1.json`, `full_narrow_streaming_b2.json`).

The batch-one / one-frame profile (`full_narrow_profile.json`) has **1,572 / 968 kernels** for encode/decode, down twelve per direction from the previous same-geometry profile. Fixed-row calls rise from 61 to **73**, while split/reduction pairs fall from 36 to **24**. There are still **129 / 128 GEMV calls**, consuming **45.26% / 51.24%** of kernel time. All disjoint matrix groups sum to **69.72% / 77.77%**; the nested small-kernel breakdown is already included. The compiler audit covers all **44 configurations**, with **40–128 registers**, zero spills and no matrix Tensor Core instructions (`narrow_resources.json`).

The fresh original/current comparison (`full_codec_narrow.json`) measures one-frame batch-one encode **46.434 / 10.339 / 7.010 ms** and decode **36.813 / 9.088 / 6.188 ms**, for original eager / original graph / current optimized graph. Direct total ratios are **6.62× / 5.95×** versus eager and **1.47× / 1.47×** versus original graphs. Batch-eight encode is **46.231 / 14.057 / 9.160 ms**, decode **37.406 / 12.509 / 7.874 ms**: **5.05× / 4.75×** versus eager and **1.53× / 1.59×** versus original graphs. All outputs and restored executions are exact. Three rotating rounds use ten single-call samples; graph copies and owned outputs are included, loading/packing/capture/restoration excluded. These totals have fresh denominators; historical incremental gains are not multiplied together.

The full suite passes **419 tests in 68.70 seconds**. The existing shape-driven tests exercise all ten changed configurations, random weights, arithmetic edge cases, native storage, hooks and graph lifetime without adding implementation-mirroring tests. This turn is **progress**: ten validated launch configurations, rejection of broader sets that regress codec latency, and new explicit-layout/storage evidence. **100× whole-model acceleration remains unmet.** The next investigations should quantify weight traffic and consider exact storage/decode or scheduling changes beyond warm-cache tile tuning; the block-format estimate is not an implemented optimization.

## Exact weight storage and reconstruction cost

After `e87f54d`, the one-frame profile still attributes 45.26% / 51.24% of encode/decode kernel time to GEMV. `weight_traffic.py` instruments logical module calls and separately records supported small-matrix shapes, then removes instrumentation before timing. At batch one / one frame there are **343 encoder and 310 decoder module-weight calls**, each to a distinct module name. Their logical FP32 weight demand is **3.549 / 3.547 GB**, of which **3.545 GB** per direction belongs to `Linear` modules. Supported small matrices account for **3.229 / 3.225 GB**; the one-row GEMVs alone account for **2.521 / 2.517 GB**. The repeated `(1,1280,5120)`, `(1,3840,1280)` and `(1,5120,1280)` shapes dominate that GEMV demand. These counts are logical bytes, not physical DRAM measurements or a bandwidth lower bound: cache effects, functional codebook products and attention state require separate accounting.

The diagnostic checks all six batch/frame/direction cases against original and restored outputs. An initial short-warmup batch-one encoder timing is 11.641 ms; a separate run with twenty warmups measures **7.006 ms**, while the logical call counts agree exactly. The initial artifact is retained as `weight_traffic_initial.json`; it is not a codec speedup denominator. The refreshed `weight_traffic.json` also measures 6.190 ms batch-one decode, 9.088 / 7.881 ms batch-eight encode/decode and 8.122 / 6.706 ms batch-one three-frame encode/decode. This is an attribution diagnostic, not a new matched original/current performance claim.

[Unweight](https://github.com/cloudflareresearch/unweight-kernels) separates BF16 sign/mantissa and exponent information, reconstructing tiles before Hopper matrix instructions. [ZipServ](https://cse.hkust.edu.hk/~weiwa/papers/zipserv-asplos26.pdf) also studies reconstruction integrated with matrix computation. These motivate avoiding a separate write of fully decoded weights, but their BF16/Tensor Core results do not validate our FP32 SIMT path. NVIDIA's [nvCOMP Device API](https://docs.nvidia.com/cuda/nvcomp/device_api.html) provides in-kernel compression/decompression primitives; its documented device datatype choices and experimental status do not establish suitable FP32 GEMV latency on SM120. No external compression package, reduced arithmetic precision or third-party kernel code is introduced here.

`packed_fp32.py` implements a lossless representation, rather than the previous size estimate. Each 256-value block preserves all 23 fraction bits and the sign, then stores exponent deltas from that block's minimum. The variable format packs **24–32 integer bits per value**, with one 64-bit header per block and one guard word for the whole payload. Its measured storage ratio on the six captured GEMV weights is **1.133–1.135×**. A second format uses **28-bit integer records** where the exponent span fits, and verbatim FP32 blocks otherwise; its ratio is **1.129–1.131×**. These are compressed encodings of the original FP32 bits, not new lower-precision floating-point formats. All decoded words match the inputs, including IEEE special-value payloads.

Two Triton kernels reconstruct weights immediately before the original cyclic-lane FP32 FMA sequence, explicit halving reduction and final positive-zero addition. A cooperative CUDA/NVRTC variant reads seven packed words for eight values and uses shuffle/funnel-shift instructions to reconstruct them. Its CUDA shuffle intrinsics also handle mixed compact and verbatim blocks in one warp. A preliminary Gluon implementation was discarded after device errors and output mismatches; the partial `packed_fp32_cooperative_initial.json` is explicitly marked incomplete and rejected. Its root cause was not established, and no performance claim uses it. The retained cooperative implementation is CUDA source with explicit `__fmaf_rn`, `__fadd_rn`, `--ftz=false` and `--fmad=false`.

The three completed probes cover **84 launch configurations**: 24 variable-width Triton, 24 fixed28 Triton and 36 cooperative CUDA configurations. Every **2,352 eager/graph stress comparison** passes across fourteen activation variants, in addition to full checkpoint-word round-trips. The variable kernels use **40–80 registers**, fixed28 kernels **40–255**, with zero reported spills; CUDA kernels use **36–40 registers** and zero local bytes. None contains matrix Tensor Core instructions. Register counts and memory size alone do not predict speed.

All three formats are rejected for runtime acceleration. Against the current kernels, the best warm variant per shape has speed ratios **0.567–0.824×** for variable-width Triton, **0.348–0.808×** for fixed28 Triton, and **0.484–0.729×** for cooperative CUDA. Corresponding evicted ratios are **0.442–0.778×**, **0.400–0.741×** and **0.455–0.800×**. Standalone decode followed by the current GEMV is also slower. These are component experiments with packing/capture excluded, not full-model speedups. The reference weights remain allocated for comparison; the storage ratios do not describe a reduction in the current model's allocated memory. The reports are `packed_fp32_probe.json`, `packed_fp32_fixed28.json` and `packed_fp32_cooperative.json`.

Thirteen new regression tests cover all exponent-width cases, mixed compact/verbatim blocks, non-contiguous source views, arbitrary raw words, NaN/infinity payloads and signed-zero underflow in direct and graph execution. They pass in **7.26 seconds**; the full suite passes **432 tests in 67.87 seconds**. Production runtime files remain byte-identical to `e87f54d`. This turn is research progress with no additional runtime speedup: exact packing saves space, but reconstruction overhead outweighs the saving in the tested GEMVs. Further work should measure actual memory-system behavior and change the data-access/computation schedule beyond these per-value decoders. The 100× whole-model goal remains unmet.

## Native FP32 GEMV with wide loads and ordered prefetch

NVIDIA's [vectorized memory access article](https://developer.nvidia.com/blog/cuda-pro-tip-increase-performance-with-vectorized-memory-access/) motivates wider loads to reduce instruction demand, while identifying alignment, register pressure and reduced parallelism as constraints. That is a scheduling hypothesis, not evidence of a codec gain. The new research-only `gemv_vector.py` keeps the original FP32 cyclic FMA chains and halving reduction tree. Each CUDA thread owns one, two, four or eight consecutive native lanes. Prefetch windows load one, two or four steps before consuming them in the original order. Explicit round-to-nearest FMA/add, disabled flush-to-zero and a final positive-zero addition preserve the established signed-zero behavior. No weight packing, reduced precision or arithmetic reassociation is introduced.

`gemv_vector_tune.json` covers **336 configurations across all six captured GEMV shapes**. All captured outputs match at the bit level; resource use is **16–40 registers with zero local bytes**. Three rotating confirmation rounds select **22 finalists** and test fifteen activation/weight variants, including minimum subnormals and forced-positive weights. All **660 candidate eager/graph comparisons** pass, plus 360 control comparisons. The initial sweep's PTX vector counter missed qualified load instructions; those incomplete counts were removed, and the separate corrected compiler audit supplies actual instruction evidence.

`gemv_vector_instructions.json` inspects the 22 finalists and two explicit wide-load examples. All eleven vectorized kernels emit actual **64-bit or 128-bit global loads**. The thirteen scalar kernels emit scalar loads. Inspected kernels use **26–40 registers**, no local loads/stores and no matrix Tensor Core instructions. The first inspection invocation failed because PyTorch had not materialized a CUDA context; allocating a CUDA tensor before the driver call fixes the tool setup. No result from that failed invocation is used.

Only the `(1,5120,1280)` projection retains a warm improvement greater than 1%. The best confirmed configuration `(lanes=8, vector=2, threads=32, prefetch=4, unroll=4)` measures **10.293 → 9.803 microseconds (1.0499×)** warm, but **40.960 → 45.056 microseconds (0.9091×)** after eviction. The other five shapes' best warm ratios are **0.923–0.983×**. Cold samples use a 128 MiB cache flush and graph replay without graph input/output copies; they are component measurements, not codec latency.

The warm winner proceeds to an independent-context full-model ablation in `full_gemv_vector.json`. All **48 cases** match original tokens, hidden-state bits, waveform bits, and restored outputs. The candidate executes in five one-frame batch-one corpus cases; the other 43 cases exercise unchanged dispatch. However, batch-one / one-frame encode regresses **7.089 → 7.122 ms (0.9953×)** and decode **6.267 → 6.298 ms (0.9951×)**. Batch-eight / one-frame and batch-one / three-frame controls never call the candidate, and vary within 0.44% of parity. Their small differences are measurement variation, not candidate acceleration. Three alternating rounds use independently restored contexts and five samples of ten graph calls, including input copies and owned outputs. Peak allocation is **7.723 GB**.

The CUDA candidate is rejected for runtime adoption. Thirteen new tests cover all four vector widths, three native lane geometries, prefetch windows, random/subnormal/signed-zero inputs, positive-weight underflow, graph replay, alignment and invalid-operand rejection. The focused suite passes **13 tests in 5.19 seconds**; the full suite passes **445 tests in 71.43 seconds**. All 23 production runtime/profile hashes match the preceding wheel. Runtime source and dispatch remain unchanged, and no additional codec speedup is claimed. This narrows the scheduling search: emitted wide loads and a warm component win are insufficient when the model consumes a sequence of distinct weights.

## Interleaved output groups and distinct-weight rings

The [CUDA Programming Guide](https://docs.nvidia.com/cuda/cuda-programming-guide/02-basics/writing-cuda-kernels.html#coalesced-global-memory-access) explains memory coalescing in terms of 32-byte segments used by a warp. The existing 8-/16-/32-lane GEMV patterns already fill such segments. This experiment tests broader address locality by interleaving neighboring output rows; it does not assume that the original layout wastes transaction bytes or claim measured DRAM counters.

`gemv_interleaved.py` packs FP32 words as `[output_group, cyclic_step, output_within_group, native_lane]`. Each step then reads neighboring outputs contiguously while preserving the full native FMA chains, reduction tree and final positive-zero addition. Packing uses integer views and retains all IEEE payload bits. Parameters remain untouched; this research implementation holds an additional FP32 copy. Groups span 1–32 outputs, with 1/2/4 warps and 4/16/32 loop unrolling. All **324 configurations** match captured outputs, use **37–160 registers**, report zero spills and contain no matrix Tensor Core instructions.

Twenty finalists pass **600 candidate and 360 control eager/graph stress comparisons**. Repeated warm gains reach **1.170×** for `(1,3840,1280)` and **1.176×** for `(1,5120,1280)`; both use four interleaved outputs. Their evicted medians are at parity with the current kernels. The eight-output expanding-FFN alternative has a slightly better evicted result. The contracting FFN retains only **1.0128×** warm improvement. These are isolated components, with packing excluded (`gemv_interleaved_tune.json`, `gemv_interleaved_confirm.json`).

Full-model tests compare the four-shape warm selection and a three-shape cold selection in separate runs. Each passes **48 cases**, matching original token, hidden-state and waveform bits in eager/graph execution and after context restoration. Five cases per run exercise the new one-row path; the remaining cases check unaffected dispatch. The warm selection improves batch-one / one-frame encode **7.097 → 7.056 ms (1.0057×)** and decode **6.273 → 6.228 ms (1.0072×)**. The cold selection gives **7.109 → 7.038 ms (1.0102×)** and **6.269 → 6.224 ms (1.0072×)**. Unaffected geometries vary by less than 0.34%. Three alternating independently restored contexts measure five samples of ten graph calls, with input copies and owned outputs included. These are incremental comparisons to the current runtime, not new upstream speedup totals.

The warm arm packs **193 weights / 4.618 GB**; the cold arm packs **192 / 4.614 GB**. Warm-arm packing takes about **30 ms** for the active corpus cases, excluded from steady-state timing. Peak allocations rise to **12.370 / 12.366 GB**. A roughly 1% codec gain does not justify this default memory cost, so neither is integrated. The sources and reports preserve the experiment for a possible future storage-ownership design (`full_gemv_interleaved.json`, `full_gemv_interleaved_cold.json`).

`gemv_weight_ring.py` adds a diagnostic with **1/4/16/32 distinct allocations**, each holding a bit-identical copy of a captured weight. This is a synthetic sequence, not 32 actual checkpoint layers. Graph timing excludes copies and packing and amortizes event overhead over the ring. All twelve shape/ring cases remain exact. At 32 allocations, QKV's apparent **1.176×** one-allocation gain falls to **1.0033×**. The expanding FFN falls from **1.188× / 1.208×** for four/eight-output packing to **1.0137× / 1.0165×**. The contracting FFN is **1.0223×**. This provides direct evidence that repeated access to one allocation substantially overstates the benefit for these weight streams; it does not by itself prove a DRAM bandwidth limit (`gemv_weight_ring.json`).

Twenty-eight new tests cover raw IEEE payloads, all group/lane combinations, non-contiguous source views, random/subnormal/signed-zero arithmetic, positive-weight underflow, graph replay and invalid contracts. The focused suite passes **28 tests in 4.25 seconds**; the full suite passes **473 in 81.06 seconds**. All 23 runtime/profile hashes match the preceding wheel. Production runtime source and dispatch remain unchanged. Further scheduling work should include distinct-allocation timing early, alongside full-model gates and actual memory-system measurements where available. The requested 100× whole-model result remains unmet.
